## 2026-08-13 (2) - Adversarial PDR #5: Clean Bill of Health, Fab-Readiness Assessment

*Status: `COMPLETED`*
*Type: `PCB` (Claude ran the PDR agent + WP hygiene cleanup; no board changes this session)*

### What Was Done
- User asked for one more adversarial review and whether the next step is moving from design into build. Ran a fifth PDR (fresh agent), this time explicitly framed to answer: is there any real reason not to order this board right now?
- **Result: clean bill of health -- 0 blocking, 0 should-fix.** This was the most thorough round yet: full pin-by-pin schematic<->PCB netlist diff across all 78 multi-pin nets (0 mismatches, the strongest possible confirmation schematic and PCB agree electrically despite the documented broken F8 link), minimum annular ring computed across all 52 THT pads (0.225mm, safely above fab minimums), board-edge copper clearance re-checked as an actually-enforced DRC rule with 0 violations anywhere on the board (not just spot-checked near the RF module like earlier rounds), and DFM checks beyond KiCad's own DRC (soldermask dam width, JLCPCB-class trace/space minimums)
- Two new minor notes, both cosmetic only: a stray disconnected wire stub in the schematic near the Q1/R2/J8 protection circuit (doesn't touch real connectivity -- verified via the full netlist re-derivation); the project's `min_silk_clearance` design rule is configured at 0.0mm, looser than typical fab recommendation (worth one more visual pass over the tightest silkscreen rows before ordering, not a solderability risk)
- **Verdict, direct quote: "No -- there is no real reason not to order this board right now."**
- Separately, cleaned up stale Work Packages bookkeeping found while assessing the broader "ready for build" question: WP4.1 still carried two unchecked duplicate lines ("PCB layout for the barrel-jack board," "confirm shell interior dimensions") that were both already done and tracked elsewhere -- closed as stale duplicates, since leaving them open was actively misleading for exactly the question being asked
- PDR #5 also flagged a deeper PM-hygiene issue: 5 sessions of active Phase 4 (barrel-jack) placement/routing/PDR work has been logged under WP3.1, a child of Phase 3 which is still headed `*(PAUSED)*` -- same failure mode as the 2026-08-07 dashboard-heuristic finding, but this time within the project's own Work Packages file. Logged as a new PM Template Improvement Suggestion rather than migrating the existing history (too risky/lossy to move now)
- Separately assessed (not part of the PDR's scope, which was PCB-only): the RF TX module (J3) still has a placeholder footprint per `PCB Design Plan.md` ("no real footprint exists anywhere... hand-draw from calipers once physically in hand") -- this is a real gate on ordering that wouldn't show up in any DRC/ERC check, since it's a sourcing/measurement gap rather than a design error. Same lower-stakes issue exists for the crystal's load-cap value (generic/placeholder until a specific crystal part is chosen). Everything else still open in Work Packages belongs to the earlier Nano-based project or the paused battery variant, not blockers for this board

### Decisions Made
- None new -- this was an assessment/verification session, no board changes

### Pending / Next Session
- **Before ordering**: confirm the RF TX module is physically in hand and its footprint has been hand-measured/verified against J3 (or order with the acknowledged risk that it might not match)
- Optional, low-priority, purely cosmetic: dangling `D6` label, the newly-found stray wire stub near Q1/R2/J8, and `min_silk_clearance=0.0` -- none block fab
- The WP3.1/Phase 3 mislabeling PM Improvement Suggestion is logged but not acted on -- fix forward (log future barrel-jack work under Phase 4 headings), don't migrate existing history
- Five rounds of independent adversarial review now complete on this board, four of which found and resolved/deferred real issues, the fifth found none -- this board is genuinely design-complete pending the RF TX module footprint confirmation

-----

## 2026-08-13 - Duplicate Track Cleanup, Adversarial PDR #4, J8 Decision, Trace Widths

*Status: `COMPLETED`*
*Type: `PCB` (mixed -- Claude ran the PDR agent + scans/DRC/git, user made all board changes)*

### What Was Done
- User suspected more duplicate tracks than the single one PDR #3 caught at U1 pad 8. Claude wrote a script comparing start/end/layer/net across every `(segment ...)` block -- found **183 of 367 total segments were exact duplicates**, roughly half the board's routing doubled. Likely accumulated across several FreeRouting-import + manual-routing passes where new tracks got added alongside old ones instead of replacing them
- Fixed via **Route -> Cleanup Tracks and Vias** ("delete redundant tracks" + "merge co-linear segments" + "delete dangling tracks"). Verified after: 367 -> 183 segments, 0 duplicates remaining, DRC unchanged (0 unconnected, same 4 harmless violations) -- confirmed no connectivity was broken
- Ran a fourth adversarial PDR (fresh agent), explicitly given the corrected axis convention this time and told to re-verify its own assumptions, not just the board. It cross-validated its coordinate math against rendered SVGs specifically to avoid repeating PDR #3's mistake
- **PDR #4 confirmed everything held**: duplicate cleanup complete, Y1's new edge clearance (1.7mm, up from ~0.70mm), ERC/DRC exact match to claimed state, no new courtyard overlaps, silkscreen/mounting-hole fixes intact, RF antenna clearance unchanged (still deferred per standing decision)
- **PDR #4 found a real blocking issue, mirroring the J2 pattern**: J8 (barrel jack) is placed at the FRONT of the board (X≈9.5-12mm), not the back the design plan requires -- it was placed under the old, wrong Y-axis convention (its Y≈36 satisfied that stale assumption) and never re-checked after the axis fix. The design plan's own claim that "J8 at the back... remains accurate" (added the same session as the axis fix) turned out to be another unverified holdover
- User reviewed and **explicitly dropped the barrel-jack-at-back constraint**: the leads come off the board normal (perpendicular) to it, not in-plane, so the "short distance across the panel" rationale behind the original constraint doesn't actually hold. Same treatment as the U1 decision -- documented in `PCB Design Plan.md`, not fixed by relayout
- **PDR #4 also flagged the +5V power path as uniformly 0.2mm** with no widened trunk for the LED strip's current draw. User widened the J8->Q1 input leg to 0.5mm (clean), then widened the rest of the +5V net (Q1->J6 trunk + U2-regulator branch) to 0.4mm -- which caused **26 DRC violations** (22 clearance), because the board is too dense for a uniformly wider net without a real reroute. Reverted the low-current U2 branch and the J6 trunk back to 0.2mm, keeping only the J8->Q1 leg wide. User's final call: LED draw is modest with a forgiving duty cycle (not sustained full-brightness), and 0.2mm at 1oz copper (~0.7-1A continuous) has real headroom for that load -- not worth fighting the board's density for more
- Also confirmed via PDR #4: the previously-tracked "misapplied zone-connect override on U1 pad 2" no longer exists in the file -- closed out as resolved (either a side effect of recent routing rework, or never actually saved)
- Final DRC: 0 unconnected pads, 4 violations (3 pre-existing cosmetic footprint-library warnings, 1 residual cosmetic starved-thermal flag on U1 pad 8) -- same clean baseline as before, unaffected by any of today's changes

### Decisions Made
- J8 (barrel jack) stays at the front -- constraint dropped, same reasoning class as the U1 decision (leads are perpendicular to the board, so position doesn't matter the way originally assumed)
- +5V trace widening settled at "input leg only" rather than the whole net -- board density doesn't support more without a real reroute, and the actual expected LED load doesn't need it
- `PCB Design Plan.md` now carries an explicit warning that its own self-reported placement claims have been wrong 3 of the last 4 review rounds and should not be trusted without independent re-verification

### Pending / Next Session
- Only remaining open item from PDR findings: the dangling `D6` global label in the schematic (low severity, unused pin)
- RF antenna proximity: still deferred, unchanged, revisit only if real-world range testing shows a problem
- Board is DRC-clean except unfixable cosmetic warnings, all four adversarial PDR rounds' real findings resolved or explicitly deferred -- genuinely close to fab-ready

-----

## 2026-08-12 (2) - Adversarial PDR #3, Axis-Convention Fix, Y1 Relocation

*Status: `COMPLETED`*
*Type: `PCB` (mixed -- Claude ran the PDR agent + FreeRouting/DRC/git, user made all board changes)*

### What Was Done
- Corrected `PCB Design Plan.md` per the last session's decision: dropped the "MCU must be at the front" constraint (user's explicit call, no compelling reason found), and fixed 1.7's false self-report about U1's position that the second PDR caught
- Ran a third adversarial PDR (fresh agent, same rigor as before): independently re-verified ERC/DRC (both matched claimed state exactly), re-checked whether the fixes from PDR #2 actually held (mounting-hole margin, silkscreen text, U1 pad 8/22 connectivity -- all confirmed holding), and re-measured the RF antenna clearance (confirmed unchanged since 2026-08-11, not better or worse)
- **PDR #3 flagged a new blocking issue**: J2 (RFID socket) appeared to be centered on the board rather than at the front, based on the reviewer's assumption that front/back mapped to the board's Y-axis (same assumption the first two PDRs and Claude had also been making)
- **This turned out to be a false positive** -- user corrected the actual orientation convention: the board's own silkscreen labels put "Front" near X=2mm and "Right" near Y=2mm, meaning **front/back is the X-axis, not Y**. J2 sits at X≈4mm (near zero) and was at the front all along. Documented this convention explicitly in `PCB Design Plan.md` §1.4 so no future review repeats the same axis mixup
- PDR #3 also found real, smaller items: Y1 (crystal) sitting only ~0.70mm from the board's right edge (tighter than the mounting-hole margins fixed last session, and DRC doesn't check courtyard-to-edge so this was invisible); a dangling `D6` global label surviving the 2026-08-06 J7 removal (low severity, pin genuinely unused); a duplicate GND track at U1 pad 8; a misapplied "solid connection" pad override that landed on U1's unused pad 2 instead of pad 22 (confirms a suspicion logged back on 2026-08-09); and confirmation that pad 22's GND fix is actually a direct pad-to-pour bond, not a track like earlier entries described (only pad 8's fix is a real track)
- User relocated Y1 for better edge clearance, noticed and cleaned up more duplicate tracks than just the one PDR #3 flagged (via **Route → Cleanup Tracks and Vias**), and manually finished routing everything -- no FreeRouting import used this round (an earlier FreeRouting attempt on the mid-edit board was explicitly discarded per user request, not imported)
- Final DRC: 0 unconnected pads, 4 violations remaining (3 pre-existing cosmetic footprint-library warnings, 1 residual cosmetic starved-thermal flag on U1 pad 8 -- same non-functional situation as before)

### Decisions Made
- Front/back orientation is the board's X-axis, confirmed via the physical silkscreen labels -- corrects a mistaken assumption that had gone unchallenged through two prior PDRs
- J2's placement needs no further action -- it was never actually wrong, just mis-evaluated

### Pending / Next Session
- Minor housekeeping from PDR #3, not yet addressed: dangling `D6` ERC label from the incomplete J7 removal, and the misapplied pad-2 zone-connect override (harmless, unused pin, but worth clearing next time in U1's pad properties)
- RF antenna proximity: still deferred, unchanged since 2026-08-11, revisit only if real-world range testing shows a problem
- Board is DRC-clean except unfixable cosmetic warnings -- effectively fab-ready

-----

## 2026-08-12 - U1 Pad 8/22 Re-Fixed: Board DRC-Clean (Cosmetics Only Remain)

*Status: `COMPLETED`*
*Type: `PCB` (GUI, Claude ran FreeRouting/DRC/git)*

### What Was Done
- User manually bridged U1 pad 8 and pad 22 [GND] at their new (post-recenter) positions, same technique as the earlier fix -- short manual tracks to the nearest valid GND pour
- Fresh DSN exported, FreeRouting run as a confirmation pass: started with 1 unrouted net (one of the two fixes needed the router to finish the last bit), converged, imported, zones refilled, saved
- DRC: 5 -> 3 violations, 1 -> 0 unconnected. **Both GND issues fully resolved** -- this time with no residual starved-thermal flag either (unlike the first pad-8 fix, which left a cosmetic flag behind). The `silk_overlap` between the "Right" label and U1's silkscreen (introduced by the recenter) is also gone
- Remaining state: **only the 3 pre-existing "footprint not in library" warnings on MH1-3** -- cosmetic, not fixable from the GUI (library-path naming mismatch, not a real defect), present since early in the project

### Decisions Made
- None new -- straightforward continuation of the prior session's fix

### Pending / Next Session
- Board is DRC-clean except for the 3 unfixable cosmetic mounting-hole warnings -- effectively fab-ready from a DRC standpoint
- Still open, carried forward: optional silkscreen stroke-width cleanup (0.1mm vs. the board's 0.15mm standard), the deferred RF-antenna-proximity risk (revisit only if real-world range testing shows a problem), and updating `PCB Design Plan.md` §1.4/§1.7 to drop the no-longer-true front/back MCU placement constraint

-----

## 2026-08-11 (2) - Adversarial PDR #2 + U1 Recenter, Text/Mounting-Hole Fixes

*Status: `COMPLETED`*
*Type: `PCB` (mixed -- Claude ran the PDR agent + FreeRouting/DRC/git, user made all board changes in GUI)*

### What Was Done
- Fixed MH1's silkscreen-clipped-at-edge warning (this was actually logged in the prior entry's addendum, included here for continuity) and confirmed shell interior dimensions: fits up to a ~45mm square, staying closer to 40x40mm by choice
- User requested a second adversarial PDR, matching the 2026-08-04 process. Claude launched a genuinely fresh general-purpose agent (not a context-sharing fork) with full project background, explicit instructions to independently re-verify ERC/DRC rather than trust prior claims, and specific things to dig into (physical layout constraint compliance, RF antenna keepout status, shared-SPI-bus routing quality, power-circuit correctness, DFM, board-size comfort)
- **PDR found 1 blocking issue**: the RF antenna keepout deleted on 2026-08-09 (to unblock FreeRouting near J3) was never actually being honored manually as intended -- GND pour fills to within 0.0mm of J3's courtyard and 3 GND vias sit only ~1.2-1.4mm away, vs. the 3-5mm spec in `PCB Design Plan.md` -- roughly 8-10x tighter than required. Concrete risk: detuned/reduced-range 433MHz antenna
- **4 should-fix items**: (1) U1's placement never actually matched the documented "MCU must be at the front with the RFID socket" constraint -- the design plan's own §1.7 claimed this was verified, but the reviewer computed U1's actual courtyard (spans nearly the full 40mm board depth for a DIP-28 at 2.54mm pitch) and found its anchor at the *back*, in the same zone as the barrel jack, not the front. This had never been genuinely checked before -- an example of the exact "claimed-verified-but-wasn't" gap this PDR process exists to catch. (2) The "Right"/"Top" orientation labels the user added were uncommitted, undocumented, and under the min-text-height rule (0.75mm vs 0.8mm) and typical fab legibility floors. (3) Mounting holes had thin edge margin (~1.4mm). (4) U1's decoupling caps sit within ~5-8mm of J3, inside the "~10mm general clearance" guidance, compounding the antenna finding
- **Verified clean, no action needed**: shared SPI bus (J2/J9) routing is actually shorter than before, not worse; barrel-jack protection circuit (Q1/R2) and regulator decoupling correct via fresh netlist check; U1 pad 8's residual thermal flag confirmed genuinely cosmetic (real bridge track exists)
- **Reviewer's read on board size**: 40x40mm reads as uncomfortably tight, not comfortable -- DIP-28 alone spans nearly the full depth, J2/J9 area is visually dense, mounting-hole margins were thin, RF corner crowded. Recommended growing to ~43-45mm to de-risk without a full relayout, since the shell was just confirmed to fit that
- User's decisions on the findings: **bumped label text to 1mm height** (stroke width left at 0.1mm, not the recommended 0.15mm); **widened mounting-hole margin to ~2.4mm** (re-verified sufficient); **decided U1 does NOT need to be at the front** -- no strong reason found for the original constraint, so re-centered U1 for maximum edge clearance in both directions instead, explicitly overriding the design-plan requirement rather than fixing placement to match it; **explicitly deferred the RF antenna proximity blocking issue** -- noted and accepted as a known risk rather than reworked now, to revisit only if real-world range problems actually show up during testing
- Centering U1 required clearing all tracks/vias again. Fresh DSN exported, FreeRouting re-run (42 -> 0 unrouted, ~2.3s, no GUI popup this time -- the `--gui.enabled=false` fix holds), imported, zones refilled
- DRC after: U1 pad 8 (starved-thermal/isolated island) and U1 pad 22 (unconnected) **recurred** at U1's new position -- same category of issue as every previous U1/J9-area move so far. New: a `silk_overlap` between the "Right" label and U1's own silkscreen outline, introduced by the recenter. 3 pre-existing footprint-library warnings unchanged

### Decisions Made
- The original "MCU must be at the front" physical layout constraint from `PCB Design Plan.md` is dropped -- user found no compelling reason for it and prioritized symmetric edge clearance instead. Worth updating the design plan doc itself at some point to stop asserting a constraint that's no longer intended (currently the doc's §1.7 self-report is simply wrong, which is exactly the kind of drift a future review should keep catching)
- RF antenna keepout violation accepted as a deferred/known risk rather than fixed immediately -- explicit user call, not an oversight. If it needs fixing later, the fix is either restoring a real KiCad keepout zone (with reroute) or manually pulling the GND pour/vias/decoupling caps back 3-5mm from J3
- Mounting-hole and text-height should-fix items were treated as worth fixing now (cheap, low-risk); the antenna and placement-constraint items were treated as judgment calls warranting a real decision rather than a reflexive fix

### Pending / Next Session
- U1 pad 8 and U1 pad 22 GND issues need the same manual-bridge-track fix as before, now at U1's new position
- Optional: bump the orientation-label stroke width to 0.15mm to match the rest of the board's silkscreen
- RF antenna proximity: deferred, revisit only if real-world range testing shows a problem
- Update `PCB Design Plan.md` §1.4/§1.7 to reflect that the front/back MCU placement constraint was dropped, so the doc doesn't keep asserting something no longer true
- Next real milestone: this was likely the last major placement/routing pass before the board is fab-ready pending the two recurring GND fixes above

-----

## 2026-08-11 - U1 Pad 8 Fixed: All 3 GND Issues Resolved

*Status: `COMPLETED`*
*Type: `PCB` (GUI, Claude ran DRC/git)*

### What Was Done
- Diagnosed why pad 8's adjacent-looking pour wasn't connected: a copper pour splits into disconnected islands wherever some obstruction pinches the remaining copper corridor down to zero width during fill -- two regions can look adjacent while being electrically separate. Pad 8 was touching the smaller, isolated island, not the main GND network
- Confirmed why re-checking spoke count/solid-connection settings (the earlier fix for J2/J9/U1-pad22) wouldn't help here: those settings only control how a pad bonds to whatever copper is *already touching it* -- they don't bridge a gap between two separate copper regions. Different category of problem
- User fixed it with a short manual GND track bridging pad 8 directly to the adjacent valid pour, saved. DRC: 0 unconnected pads -- **all 3 original GND issues (J2 pad 6, J9 pad 6/U1 pad 22, U1 pad 8) are now resolved**
- One residual flag remains: `starved_thermal`/isolated-island still shows on pad 8, because the pour's own fill geometry wasn't recomputed by adding the track -- it's a cosmetic pad-to-pour bond quality flag, not a functional defect, since the net is electrically complete via the track. Left as optional follow-up, not required
- User also fixed MH1's silkscreen-clipped-at-edge warning (moved the reference label clear of the board boundary). DRC: 5 -> 4 violations. Remaining 4: the residual pad-8 flag above, plus 3 pre-existing "footprint not in library" path warnings on MH1/MH2/MH3 (cosmetic library-naming mismatch, not a real defect)

### Decisions Made
- Accepted the residual starved-thermal flag on pad 8 as cosmetic rather than chasing a fully-clean DRC report -- the board is electrically sound (0 unconnected pads), and fixing the underlying pour-fill gap is lower priority than other open work

### Pending / Next Session
- Optional: find and fix the actual pinch point splitting the pour near U1 pad 8, for a fully clean DRC report (not required -- board is electrically correct as-is)
- Confirm ornament shell interior dimensions (WP2.2/WP3.1/WP4.1, still open, user-owned) to set final board outline target
- With all placement/routing/GND work now resolved, next real milestone is likely finalizing the board outline (pending shell dimensions) and moving toward fab-ready checks

-----

## 2026-08-10 (2) - Manual Reroute Resolves U1 Pad 22 and J9 Pad 6

*Status: `COMPLETED`*
*Type: `PCB` (GUI-driven, Claude ran FreeRouting/DRC/git)*

### What Was Done
- User made another placement adjustment, exported DSN, Claude re-ran FreeRouting -- this time it stalled at 6 unrouted nets (started with 8; previous full re-routes had always hit 0)
- Import attempt failed: the Specctra session came back corrupted in `pcbnew`. Rather than debug it, reverted the uncommitted `.kicad_pcb` back to the last good commit (`git restore`) and deleted the stale `.dsn`/`.ses` -- clean recovery, no data lost, cheaper than chasing the corruption
- User asked about fixing the stall by moving a trace to another layer mid-route -- confirmed KiCad supports this natively: **X** to start an interactive route, **V** while still dragging drops a via and hops to the other copper layer. For editing an *existing* trace: **G** (drag, keeps connections) then **V** mid-drag, or for traces already landing on vias/THT pads on both ends, right-click → Properties → change Layer directly, no via needed
- User manually rerouted the affected nets using this technique, re-exported DSN. Claude re-ran FreeRouting with the now-fixed `--gui.enabled=false` flag (see previous entry) -- confirmed clean, no popup, and reported "started with 0 unrouted nets" -- the manual fix had already completed everything
- Session imported, zones refilled, saved. DRC: 6 -> 5 violations, 3 -> 1 unconnected. **U1 pad 22 and J9 pad 6 both resolved.** Only U1 pad 8 (isolated island) remains from the original 3 real GND issues
- Documented the full placement/routing cycle end-to-end in Technical Reference (section 7) -- who does which half (user: move/export/import/save in the GUI; Claude: FreeRouting/DRC/git), plus the manual-reroute fallback and the corrupted-import recovery move, so this doesn't need to be re-derived next time

### Decisions Made
- Manual interactive routing (with layer-swap via V) is the right fallback whenever FreeRouting stalls, rather than fighting it with more passes or different DSN re-exports -- confirmed it's fast and FreeRouting correctly picks up from wherever manual routing left off
- Corrupted Specctra import → revert-and-redo is the standard recovery move now, not a one-off -- codified in Technical Reference

### Pending / Next Session
- U1 pad 8 [GND]: the one remaining real GND issue, unchanged across every pass so far (isolated island despite "Remove islands: Always" already being set on the zone) -- needs one more refill to confirm it takes effect, or a manual bridging track from the pad to the main pour
- Confirm ornament shell interior dimensions (WP2.2/WP3.1/WP4.1, still open, user-owned) to set final board outline target

-----

## 2026-08-10 - J9 Shrouded Header: Swap, Relocate, Re-Route

*Status: `COMPLETED`*
*Type: `PCB` (GUI-driven by user, Claude handled footprint research, git, and FreeRouting)*

### What Was Done
- Motivated by "6 loose jumper wires is a pain for building several of these" -- researched the standard fix: a shrouded/keyed IDC box header + matching ribbon cable, so ISP programming becomes "plug in one keyed cable" instead of getting 6 wires right every time
- Claude reassigned J9's schematic footprint to `Connector_IDC:IDC-Header_2x03_P2.54mm_Vertical` (2026-08-10, ERC-verified clean). Computed that this part's physical shroud (~10x16mm) does NOT fit at J9's original position (9,4mm) -- would overhang the board's top edge by ~1.6mm and crowd J2's courtyard -- so left PCB-side placement for the user to do visually rather than force a headless guess
- Attempted "Update PCB from Schematic" (F8) to sync the footprint change -- **this duplicated every single part on the board**. User caught it before saving and reverted cleanly (File → Revert, no data lost). Root-caused afterward: this board's PCB footprints are missing the `path` linkage back to schematic symbol UUIDs (likely a side effect of this project's history of direct S-expression edits bypassing normal netlist import), so F8 can't match existing footprints to symbols and treats everything as new. Documented as a **do-not-use** in Technical Reference -- Change Footprint(s) directly on the PCB side is the safe path for any future footprint swap here
- Also hit and resolved along the way: `pcbnew` launched standalone doesn't support F8 at all ("must launch KiCad project manager") -- relaunched via the `.kicad_pro` file instead of the bare `.kicad_pcb`; and F8 separately requires the schematic be annotated even though every part already had a normal numbered reference (harmless prerequisite, unrelated to the duplication bug)
- User swapped J9's PCB footprint via Change Footprint(s), relocated it to open space, cleared all tracks/vias, refilled zones, saved
- Fresh DSN exported, FreeRouting re-run headlessly: 42 unrouted nets -> 0, ~2.3s. Hit a new FreeRouting gotcha here too: the run took ~7 minutes longer than expected between "routing complete" (per the log) and the `.ses` actually being written -- turned out FreeRouting popped up a GUI data-collection consent dialog (reachable display via WSLg) that silently blocked the process with no CLI error. Fixed by disabling `gui.enabled`/`allow_telemetry`/`allow_contact` and setting `disable_analytics: true` in `freerouting.json`; documented in Technical Reference
- Session imported, zones refilled, saved. DRC: 6 violations -- same 3 real GND issues as before the move (J9 pad 6, U1 pad 22, U1 pad 8) plus the same 4 pre-existing cosmetic warnings. Nothing new introduced by the relocation. J2 pad 6's earlier fix held through the full reroute; U1 pad 22's "solid connection" setting from 2026-08-09 apparently never actually saved (confirmed unchanged across two full reroutes now)

### Decisions Made
- Standardizing on a shrouded IDC-6 box header for ISP programming going forward (repeatable cable-based flashing, not loose wires) -- worth it even though it required a placement move and full reroute
- F8/"Update PCB from Schematic" is off-limits for this board until/unless the path-linkage problem gets fixed some other way (not attempted -- out of scope for tonight)

### Pending / Next Session
- U1 pad 22 [GND]: re-check the solid zone-connection setting is actually applied and saved -- it's failed to stick twice now
- J9 pad 6 [GND]: unconnected again post-move, same class of issue as before -- looks like a recurring FreeRouting quirk with this specific GND pin rather than a one-off; still needs the isolated-pour-fragment theory checked or a manual bridging track
- U1 pad 8 [GND]: still the isolated-island issue, unchanged across every pass so far
- Confirm ornament shell interior dimensions (WP2.2/WP3.1/WP4.1, still open, user-owned) to set final board outline target

-----

## 2026-08-09 (3) - GND Thermal/Pour Fixes (Partial)

*Status: `COMPLETED`*
*Type: `PCB` (GUI session, Claude ran DRC checks + git between passes)*

### What Was Done
- User set J2 pad 6, J9 pad 6, and U1 pad 22 [GND] to solid zone connection (pad Properties override, bypasses the thermal-relief spoke-count requirement). DRC: 8 -> 6 violations
- **J2 pad 6: fully resolved**, no longer appears in the report at all
- **J9 pad 6: partially resolved** -- the starved-thermal flag cleared, but it's still flagged unconnected. User's working theory: it's bonded to an isolated pocket of GND pour that isn't itself connected to the main ground network, rather than a broken track (GND pads generally connect via the pour, not discrete tracks, so a "missing track" explanation didn't fit what the user was seeing). Plausible and consistent with the DRC wording, not yet confirmed
- **U1 pad 22: unchanged** -- still both starved-thermal and unconnected, identical to before the fix attempt. The solid-connection setting likely didn't actually save for this pad (28-pin DIP, easy to edit the wrong pad in the properties dialog)
- **U1 pad 8: untouched this pass** -- user visually confirmed the GND zone's "Remove islands" setting was already "Always," and separately observed via visual inspection that a piece of the copper pour does appear to physically touch/cross the pad -- consistent with the DRC finding: it's an *isolated* island (disconnected from the main GND network), not a *missing* connection, so it can look connected while still failing electrically
- Claude regenerated DRC report + SVGs and committed straight to `main` (`9749225`)
- User decided to stop here for the night and go research PCB fundamentals (pour vs. track vs. thermal relief relationships) before continuing -- explicitly wants to build intuition rather than have Claude just fix these directly

### Decisions Made
- Left all 3 remaining GND issues (U1 pad 8, U1 pad 22, J9 pad 6) open rather than attempting further headless fixes -- they need visual GUI diagnosis and the user explicitly wants to build understanding of pour/track/thermal-relief mechanics before the next attempt

### Pending / Next Session
- U1 pad 22: re-verify the solid zone-connection setting actually applied and saved
- J9 pad 6: confirm/diagnose the isolated-pour-fragment theory, then bridge it to the main GND network (likely a manual track once the actual gap is found)
- U1 pad 8: same isolated-island class of problem -- confirm whether one more zone refill (now that "Remove islands: Always" is set) clears it, or if it needs a manual bridging track
- Confirm ornament shell interior dimensions (WP2.2/WP3.1/WP4.1, still open, user-owned) to set final board outline target

-----

## 2026-08-09 (2) - FreeRouting Pass: Board Fully Routed

*Status: `COMPLETED`*
*Type: `PCB` (KiCad GUI for DSN export/session import, Claude ran FreeRouting headlessly + git)*

### What Was Done
- User exported Specctra DSN from the current (post-cleanup) board via `pcbnew`'s File -> Export -> Specctra DSN (still GUI-only in KiCad 10.0.5, no `kicad-cli` equivalent)
- Claude ran FreeRouting headlessly: `/tmp/freerouting.jar` (v2.2.4) with the JDK 25 JRE at `/tmp/claude-1000/jre25/` (the bundled JDK 17 JRE at `~/.local/opt` errors with `UnsupportedClassVersionError` -- this jar needs class file version 69 = Java 25). Invocation: `java -jar freerouting.jar -de <board>.dsn -do <board>.ses -mp 50`. Result: **42 unrouted nets -> 0**, converged in 4 passes in ~2.5s -- full improvement over the 2026-08-05/06 attempt that stalled at 2 unrouted nets at J3 (that session's antenna keepout, since removed, was blocking the router there)
- User imported the `.ses` back via File -> Import -> Specctra Session and saved
- DRC immediately after import: 52 violations (45 clearance + 3 hole_clearance, all `actual 0.0000mm` against the same GND zone) -- diagnosed as a stale zone-fill artifact (the pour wasn't recomputed against the newly-imported tracks), not real defects
- User refilled zones (**B**) and saved again. DRC dropped to 8: 4 pre-existing cosmetic (MH1 silk-edge clip, 3 footprint-library path warnings, unrelated to routing) + 4 real `starved_thermal` errors (GND pour not fully bonded to J9 pad 6, U1 pads 8 and 22, J2 pad 6) -- exactly the category of leftover work anticipated back on 2026-08-07
- Claude regenerated `drc_report.txt`/`board_top.svg`/`board_bottom.svg` and committed directly to `main` (two commits: the routed board, then this PM update via worktree per the no-PR workflow agreed this session)
- `.dsn`/`.ses` files were not tracked in git (consistent with prior practice -- they're intermediate artifacts, not deliverables)

### Decisions Made
- Stopped opening a PR for every worktree merge -- per explicit user request, Claude-authored edits still go through a worktree (required for this session's background-job isolation) but merge straight into `main` locally and push, no `gh pr create`/`gh pr merge` step. User's own GUI-made file changes go straight to `main`, no worktree at all
- Left the 4 starved-thermal GND fixes for a future session rather than attempting a headless fix -- these need visual inspection of pad/pour geometry in the GUI

### Pending / Next Session
- Fix the 4 starved-thermal/GND-pour bonding errors (J9 pad 6, U1 pads 8 and 22, J2 pad 6) -- likely a short manual stub track from each pad to the nearest zone fill, or a check of the GND zone's thermal spoke count/width settings
- Confirm ornament shell interior dimensions (WP2.2/WP3.1/WP4.1, still open, user-owned) to set final board outline target

-----

## 2026-08-09 - Placement/Pre-Route Cleanup (GUI Session)

*Status: `COMPLETED`*
*Type: `PCB` (KiCad GUI, user-driven with Claude handling git/PM)*

### What Was Done
- Launched KiCad's `pcbnew` for the user on `MagicBand_BarrelJack.kicad_pcb` (main branch). Note for future sessions: `kicad`/`kicad-cli` on PATH is `kicad-cli` only -- the GUI app is an AppImage at `~/.local/opt/kicad-10.0.5-x86_64.AppImage`, already extracted to `~/.local/opt/squashfs-root` (FUSE isn't available in this environment, so the AppImage itself won't mount -- run `squashfs-root/AppRun` or `squashfs-root/AppDir/bin/pcbnew` directly instead). Launching the GUI also required bypassing the bash sandbox (X11/Wayland socket connection was blocked under it despite `DISPLAY`/`WAYLAND_DISPLAY` being set correctly)
- User repositioned J8 (barrel jack power input) and manually resolved the U2/J2 silkscreen label overlap that was flagged as needing a GUI nudge (see 2026-08-07 entry) -- confirmed correct behavior along the way: zone fills are a cached render and don't auto-recompute when a footprint moves, need to hit **B** (Fill All Zones) after repositioning
- User deleted J3's (RF TX module) antenna keepout zone -- it was blocking FreeRouting from placing vias/tracks near it. Decision: keep other copper off the antenna's near-field manually during hand-routing instead of via a formal KiCad keepout rule
- User selected and deleted all leftover tracks/vias from the 2026-08-05/06 FreeRouting session that was closed without saving (confirmed vias aren't grouped with footprints -- moving a part with `M` leaves nearby vias behind since they're independent copper objects tied by net, not by group; Selection Filter set to Tracks+Vias only, then Ctrl+A, Delete was the clean way to clear it all)
- Refilled zones, saved. Claude regenerated `drc_report.txt`/`board_top.svg`/`board_bottom.svg` and committed: DRC 176 -> 4 violations (remaining are cosmetic: MH1 silk-edge clip, 3 pre-existing footprint-library path warnings), 42 unconnected pads (expected, board has no routing yet)
- Also provided the user a full silkscreen-ref/value/function table for the board on request, and a filtered list of connector/THT refs (J2, J3, J6, J8, J9, U1, Y1) worth prioritizing for edge access since those are the parts that get soldered by hand or interacted with physically (vs. the SMD passives)

### Decisions Made
- No formal antenna keepout zone for J3 going forward -- relying on manual copper discipline instead, since the keepout was actively blocking the router from completing routes near the RF module
- Claude owns all git pull/push per standing preference; user's own GUI-made file changes get committed directly to `main` (no worktree needed for that, since it's not a Claude file edit) -- Claude only isolates into a worktree for its own PM-doc edits

### Pending / Next Session
- Board is placement-clean and routing-clean-slate: run a fresh FreeRouting pass (DSN export -> FreeRouting -> Specctra re-import, GUI-only step)
- After FreeRouting: hand-route J3's nets (keepout removed, so this should go cleanly now -- verify antenna clearance manually), fix GND pads not bonded to the pour, fix starved-thermal GND connections
- Confirm ornament shell interior dimensions (WP2.2/WP3.1/WP4.1, still open, user-owned) to set final board outline target

-----

## 2026-08-07 (2) - PM Hygiene: Fix Stale Phase 4 Label, Log Dashboard Heuristic Gap

*Status: `COMPLETED`*
*Type: `Manual` (PM-only, no code/schematic/PCB touched)*

### What Was Done
- Investigated why the Project Dashboard card for this project was surfacing stale next-steps instead of current work. Root cause: `3. Work Packages.md`'s Phase 4 heading was still `*(Not yet started)*` despite being 4/6 tasks complete -- it's actually the active phase. Corrected to `*(In Progress)*`
- Added `1. Project Management/6. PM Template Improvement Suggestions.md` (was missing from this project) documenting a deeper dashboard bug: the dashboard's `parseNextSteps` (in `X.-Claude-Project-Dashboard`) takes the last 5 unchecked checkboxes by raw file order, which still surfaces stale items from Phase 3 *(PAUSED)* ahead of/alongside real Phase 4 work whenever a later phase's status label lags actual progress, as happened here
- Filed a companion PR against the dashboard repo itself (`X.-Claude-Project-Dashboard` PR #2, open/unmerged) logging two backlog items there: sync its CLAUDE.md doc (still says "first 5"; code has done "last 5" since 2026-08-05, commit `bab1be6`, itself never logged in that repo's own WP/session-log at the time) and make `parseNextSteps` phase-aware
- Merged to `main` here as PR #3

### Decisions Made
- Treated "fix the stale WP label" (this project, done) and "fix the dashboard's parsing logic" (a different repo's job) as separate concerns -- this project's fix was correct regardless of what the dashboard ever does with it
- Did not flip CLAUDE.md's dashboard-compat wording from "first 5" to "last 5" here or in the template -- that doc should describe whatever the dashboard's *corrected*, phase-aware behavior ends up being, not the current (also-flawed) last-5 heuristic

### Blockers / Issues
- None on this project's side

### Next Session
- No action needed here on the dashboard-heuristic front -- that's tracked in `X.-Claude-Project-Dashboard`'s own backlog (PR #2, WP1.4)
- Real next actions are unchanged from before this pass: WP3.1 (redo FreeRouting DSN export -> autorouter -> re-import from the current 188-violation board, hand-route the 2 J3 nets, confirm ornament shell interior dimensions) and WP4.1 (barrel-jack PCB layout, same dimensions confirmation)

-----

## 2026-08-07 - Finalize Placement: MH3/J6 Courtyard Fix, Silkscreen Label Nudges

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Before starting: found the local checkout had staged/unstaged/untracked changes built on a base 2 commits behind `main` (a real FreeRouting pass, but run against a since-reverted J7 connector) -- would have silently undone the J7 removal and erased 2 Session Log entries. Stashed it in the main checkout (`stash@{0}`, not applied, not dropped) rather than committing or discarding blindly. Logged the gap in a new `6. PM Template Improvement Suggestions.md` (session start routine should pull before trusting local state)
- User asked to finalize component placement before the next FreeRouting pass. Reviewed the current DRC report (188 violations) and separated placement-class issues (courtyard/silkscreen) from routing-class ones (solder_mask_bridge/shorting_items/tracks_crossing/etc., all from the old scripted routing that FreeRouting will replace wholesale)
- **Fixed the real placement error**: MH3 (mounting hole) sat inside J6's courtyard (`npth_inside_courtyard`, error-level) -- computed J6's actual rotated courtyard bounding box (X:33.2-38.7, Y:14.35-23.25 in absolute board coords, JST_PH connector rotated 90°) by hand from the footprint's local courtyard rect + placement, confirmed against the DRC violation coordinates. Surveyed neighboring footprint positions to find genuine clearance, moved MH3 from (37,20) to (37,27) -- same 3mm edge margin as MH1/MH2, ~1.65mm clear of J6's courtyard, ~10mm from MH2. This also cleared ~7 of the silkscreen warnings that were clustered at the same MH3/J6 location as a side effect
- **Fixed 2 of 3 remaining silkscreen label collisions** (leftover from the 2026-08-05 "un-hide all reference designators" pass): nudged R1's reference field 2.2mm sideways (was overlapping C6's body outline) and C4's reference field +1.43mm vertically (was overlapping C3's GND pad) -- both clean per DRC, eliminated both `silk_over_copper` violations (ink-over-exposed-copper, the more serious defect class vs. plain silk-on-silk overlap)
- **Left U2's reference-field-vs-J2-silkscreen overlap unresolved** -- tried 4 different offsets (up, down, both magnitudes, sideways both directions); every option in this specific row (C1-U2-C2, J2 above, Q1 below) traded the original silk-on-silk overlap for something worse (own-pad copper overlap, neighboring label overlap). Reverted U2 to its original position rather than ship a regression. This is a genuinely tight packing problem better solved visually in the GUI than guessed headlessly
- Regenerated `drc_report.txt`, `board_top.svg`, `board_bottom.svg` to match. Schematic untouched, so ERC/`schematic.pdf` weren't regenerated (nothing changed there)
- Net DRC result: 188 -> 176 violations. `npth_inside_courtyard` 2 -> 1 (only the separate, pre-existing MH1/J8 flag remains). `silk_over_copper` 8 -> 0. `silk_overlap` 5 -> 1 (U2/J2, open)

### Decisions Made
- Stash, don't discard or commit, uncommitted work built on a stale git base -- preserves it for inspection without risking silent data loss or reintroducing already-removed work
- Prioritized the one error-level placement issue (MH3/J6) over the routing-class warnings, since those will be wiped out and redone by the next FreeRouting pass regardless
- Stopped iterating on U2's label placement after 4 failed attempts rather than force a worse trade-off -- flagged for the user's own GUI pass instead

### Blockers / Issues
- U2 reference-field-vs-J2-silkscreen overlap still open (`silk_overlap`, 1 violation) -- needs visual GUI placement, this row has no headless-computable clean spot
- Separate pre-existing MH1/J8 `npth_inside_courtyard` conflict not addressed this session (out of scope -- user asked specifically about the MH3/J6 conflict context)
- Stashed stale-base FreeRouting work (`stash@{0}` in the main checkout) still sitting there, unresolved -- not reusable as-is since it predates the J7 removal, but not dropped in case any of it is worth referencing

### Next Session
- Redo DSN export -> FreeRouting -> re-import from this now MH3-repositioned, J7-free board -- needs the KiCad GUI (user driving, same as the 2026-08-05/06 session)
- User to visually nudge U2's reference field clear of J2's silkscreen in the GUI
- Consider dropping the stashed stale-base work (`stash@{0}`) once confirmed nothing in it is worth salvaging

-----

## 2026-08-06 (2) - Remove J7/Inner Ring from Schematic + PCB, Verified Clean

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Completed the schematic/PCB half of the LED-strip scope change deferred at the end of the prior session (KiCad had since been closed by the user, removing the live-file-conflict risk that motivated the deferral)
- Removed J7 (was the inner-ring NeoPixel connector) entirely from `MagicBand_BarrelJack.kicad_sch` -- the component itself, its D6 net wire/global-label, and its dedicated +5V/GND power symbols. Renamed J6's schematic value from `NeoPixel_Outer_16px` to `NeoPixel_LED_Strip` (count-agnostic, actual pixel count TBD pending the strip order)
- Removed J7's footprint from `MagicBand_BarrelJack.kicad_pcb` (495-line block), plus 2 dangling F.Cu track stubs on the D6 net that were routed to J7's old board position with nothing on the other end anymore
- Did this via direct file editing (Edit tool, not a scripted/blind bulk write -- the sandbox's auto-mode classifier blocked both a Python-based edit and a `sed -i` deletion for this session's file-mutation actions; the reviewable Edit-tool path is what actually went through)
- **Verified, not assumed, clean**: `kicad-cli sch erc` -- 0 errors (129 warnings, down from 209, all pre-existing `endpoint_off_grid` cosmetic noise unrelated to this change). `kicad-cli pcb drc` -- 0 unconnected pads, 0 footprint errors (188 violations total, down from 202). Confirmed a predicted side benefit: `npth_inside_courtyard` dropped from 3 to 2 -- the MH3/J7 courtyard conflict is gone, only MH3/J6 and the separate pre-existing MH1/J8 conflict remain
- Regenerated `erc_report.txt`, `drc_report.txt`, `schematic.pdf`, `board_top.svg`, `board_bottom.svg` so the tracked exports match the current file state, not stale pre-change versions

### Decisions Made
- Used direct S-expression editing rather than waiting for another GUI session -- last session's caution was specifically about the *live, unsaved* GUI conflict, which no longer applied once the user had closed KiCad. A netlist change still deserves ERC/DRC verification either way, which this got
- Deleted the 2 dangling D6 track stubs rather than leaving them -- routed copper with nothing connected on one end serves no purpose and is exactly the kind of stray-copper cleanup this project's own review discipline has flagged before

### Blockers / Issues
- None -- clean removal, verified both electrically (ERC) and physically (DRC), no open questions

### Next Session
- Redo the DSN export -> FreeRouting -> re-import routing pass from this current 188-violation, J7-free board (last time's 202->25 result was never saved, but the recipe is proven and fast)
- Nudge MH3 clear of J6's courtyard in the GUI (only one connector's courtyard to worry about now, not two)
- Hand-route the 2 nets FreeRouting couldn't finish at J3 (D8 signal, +3V3), respecting the antenna keepout
- Fix GND-pad-not-bonded-to-pour and starved-thermal issues once routing is redone
- Once the actual LED strip is ordered/known, update `LED_COUNT` in `MagicBand_BarrelJack.ino` (currently a placeholder `16`) and the BOM's TBD pricing/consumption row

-----

## 2026-08-05/06 - First Real KiCad GUI Session: WSLg Fixes, FreeRouting Autorouter, Live Routing

*Status: `COMPLETED`*
*Type: `Interactive` (live GUI session, user driving KiCad, Claude assisting via shell/file access on the same WSL machine)*

### What Was Done
- User's first hands-on KiCad GUI session (previous PCB work was all headless/scripted). Confirmed the AppImage's bundled symbol/footprint libraries exist locally (`squashfs-root/usr/share/kicad/{symbols,footprints}`) -- corrects a wrong claim repeated in this project's own docs ("no bundled footprint library"). Completed the one-time global sym-lib-table/fp-lib-table GUI setup (WP3.1 item, previously blocked on headless environments)
- **Diagnosed and fixed two real WSLg bugs**, both now documented in `4. Technical Reference.md`:
  1. GPU-passthrough rendering bug -- window opens but appears frozen, cursor disappears on hover. Root cause: WSLg's hardware OpenGL passthrough, not an app hang. Fix: `LIBGL_ALWAYS_SOFTWARE=1` on launch, now the default documented launch command
  2. A separate, narrower canvas-freeze bug (menus/cursor-coords live, but the GL viewport itself not repainting) that only resolved after a manual window resize -- noted as a known quirk, not fully root-caused
- Set pcbnew's saved window geometry directly via its config JSON (`~/.config/kicad/10.0/pcbnew.json`) to open non-maximized at a fixed size, since no `wmctrl`/`xdotool` exists in this environment to manage windows externally
- **Got the FreeRouting autorouting path working end-to-end for the first time** -- this was blocked in the 2026-08-05 handoff because `kicad-cli` has no headless Specctra DSN export (GUI-only). With the user now in the GUI: exported DSN, ran FreeRouting headlessly from the shell (same WSL machine). Found the previously-installed portable JRE was Java 17 but this FreeRouting build needs Java 25 (class file version 69) -- downloaded Temurin 25.0.4+7 JRE directly from GitHub releases (the one host this sandbox's network allowlist permits) to `/tmp/claude-1000/jre25/`. Reverse-engineered FreeRouting's actual CLI flags from the jar's bytecode strings (no bundled docs, remembered `-de`/`-do` syntax was close but unverified) -- confirmed via `-help`: `-de <input.dsn> -do <output.ses> -mp <max_passes>` triggers headless CLI mode, no GUI needed
- Ran FreeRouting capped at 50 passes: **34 unrouted nets -> 2, converged after 4 passes** (stopped itself, didn't need the full 50). Session file re-imported into KiCad via `File -> Import -> Specctra Session`
- Latest DRC after import: **25 violations + 6 unconnected pads (31 total), down from the 202-violation scripted-routing baseline**. Breakdown: the 2 FreeRouting couldn't finish are both at J3 (RF TX module) -- `D8` signal and `+3V3` power -- almost certainly blocked by J3's own antenna keepout, which FreeRouting has no concept of as an RF-reasons zone (only whatever hard keepout rule was encoded in the DSN). 4 GND pads not bonded to the pour (J9 pad 6, U1 pad 22, J3 pad 3, J6 pad 3), 2 starved-thermal GND connections (U1 pad 8, J7 pad 3, only 1 spoke vs. minimum 2), MH3 still overlapping J6/J7 courtyards (known issue, not yet nudged), rest is cosmetic silkscreen overlap mostly downstream of the MH3 fix
- Walked user through manual routing basics (interactive router, vias/layer-switching, magnetic pads, ratsnest-as-truth, DRC as the authoritative connectivity check), copper-layer color conventions, solder mask/via-tenting basics, and pulled a complete, verified 20-part reference table directly from the schematic (also surfaced that the PCB file's own `Value` fields are stale/wrong -- read from `.kicad_sch` instead of `.kicad_pcb` for real component values)
- **Scope change, decided end of session**: user is sourcing a single LED strip (outer position only) instead of the original two-ring (outer 16px + inner 11px) design -- no second/inner ring. Updated everything safe to touch without conflicting with the (then-open) KiCad session: `MagicBand_BarrelJack.ino` (removed `innerRing` entirely, merged to one `leds` object on D5, compiles clean at 8688 bytes/28% flash -- smaller than before, one less strip), `4. Technical Reference.md` (hardware table + pin map), `BOM - Wireless Ornament Variant.md`, `PCB Design Plan.md` (component list note). **Schematic/PCB themselves (removing J7, renaming J6) deliberately NOT edited this session** -- see Blockers/Issues and Next Session

### Decisions Made
- Force software rendering (`LIBGL_ALWAYS_SOFTWARE=1`) as the permanent default KiCad launch method on this WSL setup rather than chasing a full GPU-passthrough fix (`wsl --update` / Windows GPU driver update) -- unblocks work now, board is small enough that render performance isn't a real concern
- Used FreeRouting rather than continuing manual routing from scratch -- placement was already correct/validated, so autorouting was low-risk and immediately proved far more effective than the old scripted point-to-point approach (202 -> 31 violations in under 3 seconds of actual routing time)
- Deferred the J7-removal schematic/PCB edit to next session rather than hand-editing S-expressions blind at the tail end of this one -- by the time the LED-strip decision came in, the user had already closed KiCad (see below), removing the live-file-conflict risk that motivated caution in the first place, but a netlist-changing edit still deserved a focused pass with ERC/DRC verification, not a rushed tack-on right before wrapping up. Recommended doing it via the schematic editor GUI next time rather than another headless edit -- simple delete + net cleanup, keeps schematic/PCB in sync automatically, lower risk than manual S-expression surgery for this specific change

### Blockers / Issues
- **The FreeRouting/DRC-31 result was LOST, not just unsaved** -- user closed the KiCad session without saving (confirmed via mtime check both before and after close: unchanged at 2026-08-05 07:46). The on-disk board is still at the pre-session 202-violation state. Not a data-loss problem exactly (nothing was ever persisted to lose), but means next session starts the routing step over -- the FreeRouting recipe itself is proven and fast (~3 seconds of actual routing time once the DSN is exported), so this is a quick redo, not lost effort in the research sense
- The canvas-freeze-needing-a-resize WSLg bug (see above) isn't fully root-caused -- only "resize the window" is confirmed to unstick it, no confirmed permanent fix found
- MH3/J6/J7 courtyard overlap and GND-pad-not-bonded issues both still open on the (unchanged, pre-session) board, both plausibly related to the J3 antenna keepout zone's geometry -- worth checking together next session rather than treating as unrelated. Removing J7 (see below) should resolve half of the courtyard conflict as a side effect
- Schematic/PCB still show J7 (inner-ring connector) and J6 still says `NeoPixel_Outer_16px` -- doc-level scope change is captured everywhere else, but the actual KiCad files haven't caught up yet

### Next Session
- **Do the J6/J7 schematic change FIRST**, before re-running FreeRouting -- remove J7 entirely, rename J6's value to something count-agnostic (e.g. `NeoPixel_LED_Strip`), via the schematic editor GUI. No point routing around a connector that's about to be deleted
- Then redo DSN export -> FreeRouting -> re-import (fast, proven recipe, see Technical Reference for the CLI setup)
- Hand-route the 2 nets FreeRouting couldn't finish at J3 (D8 signal, +3V3), respecting the antenna keepout
- Fix the GND-pad-not-bonded-to-pour issues and starved-thermal connections (try `Fill All Zones` first, may resolve some)
- Nudge MH3 clear of J6/J7 courtyards in the GUI (may be partly moot once J7 is gone); clean up remaining silkscreen-label overlaps
- Re-run DRC after each batch of fixes, iterate toward a clean report before considering this board fab-ready
- Remember to actually save (`Ctrl+S`) this time before closing

-----

## 2026-08-05 - Close Layout PDR Findings: Keepout, J9 Reposition, Mounting Holes, Silkscreen

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Closed the adversarial layout review's findings by hand-editing `PCB/MagicBand_BarrelJack/MagicBand_BarrelJack.kicad_pcb` directly -- confirmed no `pcbnew` Python module exists in this lite AppImage install (checked, not assumed), so all geometry work was done via S-expression text editing + `kicad-cli` DRC validation, same approach used for the schematic work
- **RF antenna keepout**: replaced the old keepout (which the review found didn't even touch J3's footprint) with a real one on both F.Cu and B.Cu, sized to J3's actual courtyard + 4mm margin. Rerouted every track found crossing it as an unrelated corridor (+3V3, D11, D12, +5V -- 4 separate offending segments found and fixed, more than the review's original 1 flagged segment)
- **J9 (ISP header) repositioned**: traced U1's actual SPI pins via its rotation math, found them on the board's left side near J2 -- J9's original far-right position was on the wrong side of the board, not just far. Moved J9 to (10.5,10), directly adjacent to J2/U1
- **Mounting holes added**: 3x NPTH, 4.2mm diameter (oversized vs. standard M3 clearance, per user's heat-stake attachment method), placed where genuine clearance existed on this dense board
- **Silkscreen**: un-hid all 20 component reference designators (were previously all hidden -- real hand-assembly risk per the review, given several visually-identical parts)
- **MFRC522/crystal clearance**: checked distance (28.5mm), documented as lower-risk than initially flagged but not fully resolvable until the real module is purchased

### Decisions Made
- Reported the honest DRC delta rather than a curated one: total violations went from 139 to 202 after this pass. Every individual fix is real, but the J9 reconnection's actual routed length came out *longer* than before (69.9-74.9mm vs. original 51-59mm) despite the placement itself being genuinely closer -- the hand-routed detour around J9's tight footprint cost more than the shorter distance saved. Recorded plainly rather than claimed as a routing improvement it isn't yet
- Kept 3 mounting holes, not 4 -- a 4th corner near J2 didn't have real clearance on this board size

### Blockers / Issues
- Routing (point-to-point signal/power nets) is still not DRC-clean, same root cause as before (no autorouter, no GUI, no `pcbnew` in this environment) -- needs FreeRouting or manual GUI completion
- Ground pour's stored fill polygon is stale relative to the new keepout zone -- needs a GUI "Refill All Zones" pass, no headless equivalent available
- New silkscreen label collisions from un-hiding all 20 references at default positions -- needs manual repositioning pass

### Next Session
- Attempt FreeRouting (Specctra DSN export/import) or hand off to the user for GUI routing completion
- GUI zone refill for the ground pour
- Manual silkscreen label repositioning to clear new collisions
- Revisit mounting hole diameter/positions once real shell boss dimensions (WP2.2) and a finished routed board are both known

-----

## 2026-08-04 (later) - Fix MOSFET Source/Drain Swap, Close Minor Review Items

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Second-round adversarial review found Q1 (reverse-polarity protection MOSFET) wired with Source/Drain backwards vs. the real AO3401A datasheet -- pin2 was treated as Drain, pin3 as Source, but the real part is Pin1=Gate/Pin2=Source/Pin3=Drain. Parent session independently confirmed this by rendering the actual AOS datasheet PDF locally (pymupdf, no poppler/root needed) and reading the pin diagram directly, rather than trusting either the original circuit's assumption or the review's claim
- Fixed: swapped which net lands on Q1 pins 2/3 (pin2->raw J8 lead, pin3->protected +5V), corrected the Value label. Verified via netlist export before and after -- ERC alone cannot catch this class of bug. ERC re-run clean: 0 errors, 209 warnings, no regression
- Closed two remaining minor items from the review: fixed two more stale `SOT-23-5` doc references in `PCB Design Plan.md`'s archived V1 footprint table (an earlier pass had only caught two of four); added a 50ms idle delay to `MagicBand_BarrelJack.ino`'s polling loop, since it had been running the MFRC522 through a full soft-power-up/down cycle every iteration with zero gap

### Decisions Made
- None new -- this was a fix pass closing findings from the prior review round, not new design decisions

### Blockers / Issues
- None

### Next Session
- PCB layout for `PCB/MagicBand_BarrelJack/` has not started yet -- schematic is now through two adversarial review rounds and should be solid; layout is the next real chunk of work
- Everything else from earlier open items still applies (source MFRC522 module and barrel jack, solder the RF TX antenna, confirm shell dimensions)

-----

## 2026-08-04 (layout) - PCB Layout: 40x40mm Placement Achieved, Routing Incomplete

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Built the PCB layout for the active barrel-jack board via the `pcbnew` Python API (no GUI available headless; `kicad-cli` itself has no netlist-to-PCB or autorouter capability, so this was done via direct board-object scripting) -- fetched all 12 distinct real footprint files from KiCad's official repo first (the local AppImage install has no bundled footprint library)
- Placed all 20 components with **zero courtyard overlaps**, fully within a **40mm x 40mm** outline -- the user's aggressive target, hit on placement. Iterated through several placement revisions to resolve overlaps found via a programmatic courtyard-bbox collision check (not eyeballing)
- Two passives (R1, R2) and one cap (C5) had bulky THT footprints inconsistent with the rest of the board's SMD parts -- swapped to SMD (R_0603, C_0805) specifically to make 40x40mm achievable; re-verified ERC clean after each swap
- Confirmed all stated physical constraints respected: MFRC522+MCU at front, barrel jack at back, RF TX module at a corner with keepout, regulator near its loads
- Routed GND as a copper pour/zone (standard practice for a high-pin-count net); attempted point-to-point routing for +5V/+3V3/signal nets via nearest-neighbor-ordered scripted tracks
- **Routing is not DRC-clean**: 139 violations remain after multiple iteration rounds (down from an initial 240). Diagnosed the root cause rather than guessing: scripted point-to-point routing can't route around nearby unrelated pads, especially THT pads which span both copper layers regardless of which layer a trace is drawn on -- this is a real limitation of the tooling available in this headless environment, not a placement or design problem
- Tried and rejected one hypothesis mid-session: moving all signal/power routing to B.Cu (reasoning: fewer SMD pads there) actually made unconnected-item count worse, confirmed via DRC re-run, because THT pads span both layers anyway -- reverted to the better-performing F.Cu approach

### Decisions Made
- Swap R1/R2/C5 to SMD footprints -- functional/electrical no-op, real space savings, needed to hit the 40x40mm target
- Report the routing gap honestly rather than either (a) claiming false completion or (b) endlessly iterating scripted point-to-point routing past the point of diminishing returns -- matches this project's established review/verification discipline

### Blockers / Issues
- Routing needs either a real autorouter (Specctra DSN export -> FreeRouting -> re-import) or manual routing in KiCad's GUI -- neither was available in this headless session
- Board thermal-relief/zone-fill nuances (a few `starved_thermal` warnings seen in one iteration) not fully investigated given the above is the bigger open item

### Next Session
- Route the board for real -- either set up FreeRouting (Java-based, can likely run headless via its own CLI) and feed it a Specctra DSN export, or do it manually in a GUI session
- Once routed and DRC-clean, re-run the full independent-review discipline (Design Plan section 3) before treating this as fab-ready

-----

# Session Log

-----

## 2026-08-04 (6) - PDR Gap Closure: Reverse-Polarity Protection, Firmware Trim

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Closed every gap found in the adversarial PDR run against `PCB/MagicBand_BarrelJack/MagicBand_BarrelJack.kicad_sch`:
  - **Blocking, fixed**: added a P-channel MOSFET (Q1, AO3401A) ideal-diode reverse-polarity protection circuit ahead of the regulator and MCU. The barrel jack is bare wire leads with no mechanical keying -- a real risk of a reversed or wrong-voltage adapter with nothing previously in the way. Chose a MOSFET over a simple series diode specifically to avoid eating into the 16MHz ATmega328P's voltage headroom (~0.3-0.7V diode drop vs. tens of mV for the MOSFET). Topology cross-checked against real references (circuitdigest.com, components101.com) before committing, not just self-derived. Hit real geometry/connectivity issues while hand-authoring the schematic (a symbol pin's true offset didn't match initial assumptions, and removing an old power symbol orphaned an unrelated PWR_FLAG's wire) -- both caught via ERC and fixed, not shipped broken. One residual ERC error needed a second real fix (adding an explicit PWR_FLAG on the protected rail) before reaching a genuine 0-error state -- confirmed by independently re-deriving net membership via the exported netlist at each step, not by trusting ERC's summary line alone.
  - **Should-fix, all closed**: removed an orphaned `D7` global label (leftover from the removed MOSFET NeoPixel-gating circuit) and properly marked the now-unused MCU pin with an explicit no-connect flag rather than leaving it silently dangling; linked the front/back physical layout constraint directly into the WP4.1 task text so it can't be missed by whoever picks up layout; documented the MFRC522/ISP shared-SPI-bus gotcha (unplug the RFID module before ISP programming); made the MCP1700-keep decision explicit (correct part, no forced swap just because its original battery-driven selection reason no longer applies); fixed a doc typo (MCP1700 footprint mislabeled "SOT-23-5" in the plan, actually SOT-23).
  - **Firmware, treated as a real fix not just cleanup**: wrote `MagicBand_BarrelJack/MagicBand_BarrelJack.ino`, a new sketch derived from `MagicBand_Wireless.ino` with the sleep/wake watchdog loop and MOSFET-gating calls fully removed -- the PDR correctly flagged the leftover ~1 second poll delay as a real UX regression (zero remaining upside without a battery to conserve), not just stale code. NeoPixel `.begin()` calls moved from per-tap to `setup()`, since pixels are now continuously powered rather than gated on/off per animation. Compiles clean: 8792 bytes (28%) flash, 443 bytes (21%) RAM.
- Final ERC: 0 errors, 209 warnings (same cosmetic categories as every other board this session -- grid alignment, missing library-table config).

### Decisions Made
- P-channel MOSFET ideal-diode over a simple series diode for reverse-polarity protection -- voltage headroom matters at 16MHz, a diode's drop doesn't.
- MCP1700 regulator kept as a conscious choice, not inertia -- no compelling reason to churn the design for a marginally cheaper part with no functional benefit.

### Blockers / Issues
- None outstanding from this PDR cycle. PCB layout for this board is still the next open task (WP4.1), now proceeding against a schematic that's been through both an independent design review and a full gap-closure pass.

### Next Session
- PCB layout for `PCB/MagicBand_BarrelJack/`, following the documented front/back physical constraint
- Still open: ornament shell interior dimensions (blocks final board outline), sourcing the actual MFRC522 module and barrel jack, soldering the RF TX antenna and confirming real outlet response

-----

## 2026-08-04 (5) - Barrel-Jack Board Schematic

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Built the active barrel-jack/wall-powered schematic (`PCB/MagicBand_BarrelJack/`), derived from the paused v2 fully-custom battery schematic: kept the ATmega328P-P circuit, MFRC522 stacking socket, RF TX wiring, and MCP1700-3302 regulator unchanged; removed the MOSFET NeoPixel-gating circuit (Q1/R2/R3, `GND_SW` net) since it existed purely for battery-life management; added a barrel-jack power input per the original wired-build spec
- Hit and fixed a real bug before committing: first attempt used KiCad's actual `Connector:Barrel_Jack` schematic symbol, but its pin geometry didn't match where the surrounding wires expected to connect -- both jack pins ended up on the +5V net with GND left disconnected from the jack (a genuine short/miswire). ERC reported 0 errors throughout, since ERC checks connectivity rules, not whether a net's *membership* matches design intent -- this was only caught by manually inspecting net contents in the exported netlist, not by any automated check. Reverted to a generic 2-pin connector symbol (same deferred-footprint-precision convention already used for Pro Mini/RF TX) with the original, already-proven wire coordinates, re-verified both pins land on separate correct nets
- ERC-clean: 0 errors, 194 warnings (same cosmetic categories as prior boards)

### Decisions Made
- Barrel jack represented as a generic connector symbol with a descriptive Value, not a precise vendor symbol -- real footprint selection deferred to layout stage, consistent with how Pro Mini and the RF TX module are already handled, and specifically because the "precise" symbol just caused a real bug
- MCP1700-3302 kept even though its original selection criterion (battery quiescent current) no longer applies -- it still works fine, no reason to force a part swap

### Blockers / Issues
- None outstanding for the schematic itself. Firmware (`MagicBand_Wireless.ino`) still has the sleep/wake watchdog loop and MOSFET-gating calls that no longer apply to this board -- needs updating separately, tracked in Work Packages

### Next Session
- Update firmware for the barrel-jack board (drop sleep/wake + MOSFET gating code)
- PCB layout for this board
- Still open: ornament shell interior dimensions (WP2.2) for board outline sizing

-----

## 2026-08-04 (final) - Pivot: Pause Battery Variant, Return to Barrel-Jack Power

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- User flagged a real business/regulatory concern: LiPo batteries carry shipping/hazmat friction (UN38.3 testing, carrier dangerous-goods rules) and marketplace listing restrictions (Etsy) if this ornament becomes a sellable product -- decided to pivot away from the battery-powered wireless variant rather than build around that risk
- Archived the wireless-variant work in place (not deleted): `BOM - Wireless Ornament Variant.md` and `PCB Design Plan.md` both marked PAUSED with full reasoning in their status headers; `2. Project Overview.md`'s Decisions Log and Status updated; Work Packages' WP2.3 and Phase 3 marked paused, new Phase 4 stub opened for the barrel-jack-powered board
- Confirmed the project already has a wall-powered architecture on record from before the wireless pivot ever happened: `MagicBand_v1.ino`, 5.5mm/2.1mm barrel jack, 5V 3A wall adapter -- documented in `4. Technical Reference.md`
- Flagged for the next session: pivoting off battery power removes the *need* for a lot of today's battery-specific engineering (Pro Mini sleep/wake watchdog loop, MOSFET-gating NeoPixels for battery life, MCP1700's low-quiescent-current selection) -- RFID wiring, RF TX/RX work, NeoPixel driving, and the JST-PH connector-fit catch all carry forward regardless

### Decisions Made
- Pause (not abandon) the battery variant -- preserved as reference in case the constraint changes later
- New wall-powered board work goes in a new Phase 4, not by overwriting the paused v1/v2 schematics

### Blockers / Issues
- None -- clean pivot, everything committed

### Next Session
- Re-derive a schematic for the barrel-jack/wall-powered board, dropping the battery-specific complexity that's no longer needed
- Also mid-conversation: user is cutting their own WS2812B LED strip (144/m, 5V, individually addressable -- confirmed protocol-compatible with existing firmware) instead of buying pre-made NeoPixel rings; connector choice (JST vs. direct solder) for the LED strip connection still open

-----

## 2026-08-04 (4) - MOSFET Circuit Integrated Directly into v2 Schematic

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- User requested integrating the MOSFET driver directly onto the board rather than keeping it as an external Adafruit module -- reasonable since it is basic, well-understood switch circuit design, unlike the RFID/RF modules which stay external
- Replaced J4/J5 (external module connectors) in `PCB/MagicBand_FullyCustom/MagicBand_FullyCustom.kicad_sch` with Q1 (N-channel logic-level MOSFET, e.g. AO3400A, SOT-23 footprint, generic placeholder symbol per this project's established convention), R2 (100R gate series resistor), R3 (10k gate pull-down for a defined-off startup state)
- Same low-side-switching topology preserved (V+ passthrough, GND_SW switched return) -- verified via exported netlist, not just ERC passing, that J6/J7 (NeoPixel headers) still connect to GND_SW correctly, now through Q1's drain
- ERC re-run clean: 0 errors, 208 warnings (was 206; +2 from the new parts, same cosmetic categories)
- Removed the prior PCB layout attempt (`.kicad_pcb`, DRC report, netlist) as stale -- it was routed against the old J4/J5 netlist and needs a fresh pass against this schematic

### Decisions Made
- MOSFET integrated directly (Q1/R2/R3) rather than kept as an external module -- removes a connector + module from the BOM at low risk, unlike the RFID/RF modules which were explicitly ruled out for this treatment (real antenna engineering)

### Blockers / Issues
- None new -- PCB layout for v2 needs to be redone against this updated schematic (tracked)

### Next Session
- Redo v2 PCB layout (placement/routing/DRC) against the MOSFET-integrated schematic
- Confirm the SOT-23 pad-to-Gate/Drain/Source mapping for the specific MOSFET part chosen before finalizing layout

-----

## 2026-08-04 (3) - V2 Fully-Custom Schematic: Built and ERC-Clean

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Built the actual KiCad v2 "fully-custom" schematic (`PCB/MagicBand_FullyCustom/MagicBand_FullyCustom.kicad_sch` + `.kicad_pro`) per the user's explicit ask to build out the fully-custom path too, not just plan it
- Replaced v1's Pro Mini header with a bare ATmega328P-P (DIP-28) circuit: 16MHz crystal + 22pF load caps, VCC/GND + AVCC/GND decoupling, bulk cap, AREF decoupling (not driven externally), 10k RESET pull-up, 6-pin AVR ISP header sharing SPI lines with MFRC522. MFRC522/RF TX/MOSFET driver/NeoPixel headers/5V input/MCP1700 regulator carried over from v1 unchanged, including v1's independent-review decoupling fix (10uF in / 1.0uF out, applied here directly since it was already flagged as a known follow-up)
- Fetched the real `ATmega328P-P` KiCad library symbol from the official `kicad-symbols` GitLab repo (it migrated from the old flat-file GitHub layout to per-part files under `.kicad_symdir` directories — had to discover this via the GitLab file-tree API rather than guessing a raw URL)
- Hit a real, non-obvious bug: KiCad's `(extends "...")` symbol inheritance (`ATmega328P-P extends ATmega48PV-10P`) doesn't resolve pins in this headless/no-library-table `kicad-cli` context — the symbol's metadata (Value, Footprint, Description) loads fine, but none of its 28 pins show up in ERC or the netlist at all. Fixed by flattening the variant into a standalone symbol (copying the base's full pin/graphic content under the variant's name, dropping `extends`) — same workaround the v1 build already used for MCP1700, now confirmed as a general necessity, not a one-off
- Hit a second, more consequential bug: naively computing a placed symbol's absolute pin position as `placement + local_offset` (matching the library's authored `(pin ... (at x y angle))` values) is **wrong** — `kicad-cli`'s engine actually resolves it as `placement_x + local_x, placement_y − local_y` (Y negated, X unaffected). This doesn't show up as an ERC error (a schematic can be internally self-consistent but still wired to the wrong physical pins and pass ERC clean) — caught it by noticing a `Device:C` capacitor's two pins were swapped from what the raw library text implied, then confirmed the same pattern via isolated single-symbol probe tests (place one symbol, wire a distinctly-named global label to a guessed coordinate, export the netlist, read back which real pin the engine associated with that point) across every symbol type used, including a re-check of `Regulator_Linear:MCP1700x-300xxTT` and `Connector_Generic:Conn_01x11` against v1's own already-committed, working file as ground truth (both confirmed the same negation rule — an earlier manual read of v1's coordinates, done by eye while tracing a long file, had been a mistake, not evidence against the rule)
- After both fixes: **ERC-clean, 0 errors**, 206 warnings (same cosmetic categories as v1 — grid alignment, missing library-table config)
- Went a step further than "ERC clean" (which only checks connectivity, not correctness against intent): exported the netlist and checked every net's actual membership against the intended design — all 9 Arduino-pin-to-ATmega-physical-pin mappings (D4-D13), MCU_RESET, GND_SW, and all three power rails (+5V, +3V3, GND) matched exactly, including the tricky shared SPI lines (D11/D12/D13 correctly tie together MFRC522, the ISP header, and the chip) and both chip GND pins (8 and the hidden duplicate 22) correctly co-resolving
- Exported `schematic.pdf` and sent it for review

### Decisions Made
- Flatten `extends`-based variant symbols rather than relying on inheritance, for any future KiCad work in this headless environment — inheritance silently fails to resolve pins here
- Treat "ERC clean" as necessary but not sufficient — always independently verify the exported netlist against design intent, since a coordinate-system bug can produce a fully self-consistent but electrically wrong schematic that still passes ERC

### Blockers / Issues
- None outstanding for the schematic itself. PCB layout (footprint placement, routing, RF antenna keepout) not started for either v1 or v2 — next open item
- The coordinate-system finding (Y-negation) applies to any future from-scratch KiCad schematic work in this project — documented in `PCB Design Plan.md`'s V2 section so it doesn't need rediscovering

### Next Session
- Set up sym-lib-table/fp-lib-table for the PCB project (needed before any GUI-based layout work)
- Begin PCB layout for v1 and/or v2: footprint placement, routing, RF antenna keepout around the TX module
- Still open: confirm ornament shell interior dimensions (WP2.2) before finalizing board outline; order Pro Mini/Lipo Rider Plus/MOSFET driver/battery for the physical prototype

-----

## 2026-08-04 (2) - Independent Schematic Review, MCP1700 Decoupling Fix

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Ran an independent review pass on `PCB/MagicBand_Carrier/MagicBand_Carrier.kicad_sch` -- a different agent than the one that built it, checking against real datasheets/reference material rather than just internal consistency, per the user's ask for the most thorough process available before locking anything in
- Confirmed correct (verified against real sources, not just re-reading the schematic): MOSFET driver low-side-switching topology (matches Adafruit's own PID 5648 docs -- V+ passthrough, switched ground return), MFRC522 pin wiring (matches a real RC522 breakout's pin order and `MagicBand_Wireless.ino`'s documented map exactly), power/ground net separation (exported and inspected the actual netlist -- GND and GND_SW share zero nodes, no crossed rails, only J2 pin 5/IRQ is unconnected and that's intentional)
- Found one real problem: MCP1700-3302's decoupling caps (C1 input, C2 output) were both 100nF. Microchip's datasheet (DS20001826, "Typical Application Circuit") specifies **1.0uF as the standard/minimum output cap** for stability -- 100nF is an order of magnitude under spec, and the MFRC522's active-scan current step (13-26mA) is exactly the kind of load transient that stresses that margin
- Fixed: C2 (output) -> 1.0uF per datasheet; C1 (input) -> 10uF, typical reference-design sizing for this part (not itself a stability requirement, but standard practice for transient headroom on the input side)
- Re-ran ERC after the fix: still 0 errors, same 120 cosmetic warnings (grid-alignment + missing lib-table config, unchanged from before) -- confirms the value change didn't introduce anything new
- Re-exported `schematic.pdf`
- Checked whether `PCB/MagicBand_FullyCustom/` (v2, bare ATmega328P) existed yet to apply the same fix there -- it didn't exist at the time of this fix, so v2 still needs the identical C1/C2 correction applied once its schematic lands

### Decisions Made
- Independent review (separate from whoever builds a given schematic revision) is now the standard step before moving any board revision to layout -- caught a real, datasheet-verifiable problem the builder's own ERC pass couldn't catch, since ERC checks connectivity rules, not component value correctness against a part's actual datasheet

### Blockers / Issues
- v2 (`PCB/MagicBand_FullyCustom/`) still needs the same MCP1700 decoupling fix (10uF in / 1.0uF out) applied once its schematic exists -- flagging so it isn't missed when that lands
- PCB layout/routing still not started -- this session was review + a targeted fix only

### Next Session
- Apply the same C1/C2 fix to v2's schematic once built
- Begin PCB layout for v1: sym-lib-table/fp-lib-table setup, footprint placement, routing, RF antenna keepout around J3 (all still pending per the prior session's notes)

-----

## 2026-08-04 - V1 Carrier-Board Schematic: Built and ERC-Clean

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Built the actual KiCad v1 carrier-board schematic (`PCB/MagicBand_Carrier/MagicBand_Carrier.kicad_sch` + `.kicad_pro`) -- real file creation via kicad-cli, not GUI clicks, per the user's explicit "you do it" direction
- All components from the PCB Design Plan present and wired per `MagicBand_Wireless.ino`'s pin map: J1 Pro Mini (11 used pins), J2 MFRC522 (8 pins, IRQ correctly left no-connect), J3 RF TX module, J4/J5 MOSFET driver in/out, J6/J7 NeoPixel outer/inner headers, J8 5V power input, U1 MCP1700-3302 with C1/C2 100nF decoupling caps
- **ERC-clean: 0 errors.** Took real debugging to get there -- three distinct bugs found and fixed along the way, all worth remembering for next time (full detail in commit `TBD`):
  1. Embedded library symbols must be renamed to `"LibraryNickname:SymbolName"` to match the `lib_id` instances reference -- otherwise KiCad silently fails to resolve pin geometry (defaults every pin to the symbol's placement origin) with no obvious error pointing at the real cause
  2. Even-numbered `Conn_01xNN` connectors do NOT use the same symmetric pin-spacing formula as odd-numbered ones (confirmed by reading the actual library files, not assuming) -- N=2's pin 1 is at local y=0, not the guessed +1.27
  3. **Library symbol coordinate space is Y-up; schematic sheet space is Y-down** -- a pin's real absolute Y is `sheet_y - local_y`, not `sheet_y + local_y`. This one silently broke every non-zero-Y pin across the whole file and took the longest to isolate (confirmed via an isolated single-connector test case, not by guessing against the full 94-item file)
- Remaining 120 ERC items are all warnings, all benign: grid-alignment cosmetics (`endpoint_off_grid`, from arbitrary placement coordinates not snapped to KiCad's default grid) and missing project-level sym-lib-table/fp-lib-table config (cosmetic for future GUI editing -- every symbol used is already fully embedded in the schematic file itself, so this doesn't affect validity)
- Added `power:PWR_FLAG` on the 5V and GND nets at J8 (the board's power input) -- textbook fix for "no driver on this net," since 5V/GND legitimately originate off-board from the external Lipo Rider Plus, not from anything visible in this schematic
- Exported `schematic.pdf` for visual review (rendered and visually confirmed net-by-net against the pin map)

### Decisions Made
- None beyond what the PCB Design Plan already specified -- this session executed that plan, didn't revise it

### Blockers / Issues
- Project lacks a proper sym-lib-table/fp-lib-table -- fine for kicad-cli validation (everything's embedded), but should be added before opening this in the KiCad GUI for layout work, so symbols/footprints resolve cleanly from the real library paths instead of only the embedded cache
- PCB layout/routing not started -- this session was schematic capture only, per the original stage plan

### Next Session
- Add sym-lib-table/fp-lib-table pointing at `~/.local/opt/squashfs-root/share/kicad/{symbols,footprints}` (or wherever KiCad ends up installed) so the GUI resolves libraries properly
- Begin PCB layout: place footprints, route traces, apply the RF antenna keepout guidance from the PCB Design Plan around J3
- Snap component placement to grid to clear the cosmetic `endpoint_off_grid` warnings (optional, doesn't block layout)

-----

## 2026-08-03 (3) - Full Recovery Runbook, Wrap-up

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Expanded the "Dev Environment" section in Technical Reference into a complete, ordered recovery runbook (SSH repo access -> arduino-cli/core/library install -> USB passthrough -> quick-reference commands + FQBNs -> dead-ends list) -- goal is that a nuked WSL2 instance or a new machine can get back to "flashing sketches from a WSL terminal" from absolute zero, no re-derivation needed
- Walked through what each BOM line item in `BOM - Wireless Ornament Variant.md` actually does and why (Pro Mini vs. Nano's CH340 sleep-current floor, MOSFET gate vs. NeoPixel off-state leakage, Lipo Rider Plus's dual charge+boost role) -- conversational, no doc changes
- Confirmed direct `git push origin main` works from this session (no PR needed) -- dropped the worktree+PR+merge pattern used earlier this session per user preference; using direct pushes going forward

### Decisions Made
- Work directly against the shared `main` checkout and push directly, rather than the worktree/PR/merge pattern used at the start of this session -- user's explicit, repeated preference; PR overhead isn't wanted for this repo

### Blockers / Issues
- None new this session -- RF_TX_Test.ino still needs the antenna soldered before real outlet-response confirmation (existing item, tracked in WP2.3)

### Next Session
- Solder antenna onto the TX module, flash `RF_TX_Test.ino`, confirm the outlet actually responds to 'o'/'f' over Serial
- Depending on what's wired on the bench: either continue with the isolated TX test, or (if MFRC522 + both NeoPixel rings are already wired to the spare Nano) skip straight to flashing `MagicBand_Wireless.ino` for a full tap-to-toggle bench test
- Still open: order Pro Mini, Lipo Rider Plus, MOSFET driver, battery -- the actual prototype build is blocked on this regardless of RF/logic progress

-----

## 2026-08-03 (2) - RF Code Capture, Wireless Firmware Codes Filled In

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Got a real Arduino board connected end-to-end through WSL for the first time this session — see the new "Dev Environment: Arduino USB Access from WSL2" section in Technical Reference for the full setup (`usbipd` bridge from Windows, `dialout` group, working from a plain interactive WSL terminal rather than fighting Windows/Arduino IDE UNC-path quirks)
- Flashed `RF_Sniffer.ino` to a Nano wired to the D-FLIFE RX module and captured codes for all 4 rows on the paired remote (3-outlet pack + an ALL row; all 32-bit, protocol 2, consistent/non-rolling across repeated presses):
  - Top: ON `0x885A8F00` / OFF `0x845A8F00`
  - Mid: ON `0xA05A8F00` / OFF `0xA85A8F00`
  - Bottom: ON `0x905A8F00` / OFF `0x985A8F00` (OFF reading identical to the captured "ALL OFF" code — likely a mislabeled row during capture, not investigated further since it's not the row in use)
  - All: ON `0x935A8F00` / OFF `0x985A8F00`
  - Pulse length measured ~695-704us across every capture — rc-switch's protocol-2 library default (650us) doesn't match, so `setPulseLength()` is now called explicitly with 700us in both firmware files instead of relying on the library default
- Confirmed which outlet is physically wired to the tree lights: **top row**. Filled `RF_CODE_ON`/`RF_CODE_OFF`/`RF_BITS`/`RF_PROTOCOL`/pulse length into `MagicBand_Wireless.ino` and `RF_TX_Test.ino` using the top-row values; both compile clean (Pro Mini and Nano FQBNs respectively)

### Decisions Made
- Explicit `setPulseLength(700)` added to both sketches rather than trusting the rc-switch library's protocol-2 default (650us) — real hardware measured consistently higher across every capture

### Blockers / Issues
- `RF_TX_Test.ino` hasn't been flashed and fired against the real outlet yet — codes are filled in and compile clean, but the TX side (does the outlet actually respond) is still unverified
- Mid/bottom/all-row codes are recorded above for reference but unused in firmware — only the top row matters for this build

### Next Session
- Flash `RF_TX_Test.ino`, confirm pressing 'o'/'f' over Serial actually toggles the real outlet
- Once confirmed, flash `MagicBand_Wireless.ino` and validate the full tap-to-toggle flow on the spare Nano (Pro Mini/Lipo Rider Plus/battery still not ordered — RF/logic side can be bench-tested on the Nano in the meantime)

-----

## 2026-08-03 - WSL Toolchain Setup, Compile Verification

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Session ran in a background job on the WSL2/Linux side of the dev environment (separate from the Windows/PowerShell side used in the 2026-07-12 sessions, where `arduino-cli` was installed via winget and flashing happens over COM5)
- Found the local `main` checkout was 8 commits behind `origin/main` — still had the deleted `Revisions/` tree (Rev3/Rev4/Sniffer) and none of the wireless-variant work. Local checkout is read-only in this job's sandbox so it couldn't be fast-forwarded from here — **user should run `git pull` on the local checkout before the next session**
- Installed `arduino-cli` (official install script, binary-only, no winget needed on Linux), `arduino:avr` core, and the four required libraries (`MFRC522`, `Adafruit NeoPixel`, `rc-switch`, `DFRobotDFPlayerMini`) — this WSL environment now has an independent toolchain, so sketches can be compile-checked here without needing the Windows side
- Compiled all four current sketches clean against their real target boards: `MagicBand_Wireless.ino` (Pro Mini and Nano FQBNs, 8844 bytes flash / 444 bytes RAM — matches the 2026-07-12 figures), `MagicBand_v1.ino` (Nano, 11042 bytes flash), `RF_Sniffer.ino` (Nano, 3408 bytes flash), `RF_TX_Test.ino` (Nano, 4228 bytes flash)
- Confirmed no USB/serial devices are visible from WSL (`arduino-cli board list` → none, no `/dev/ttyACM*`/`/dev/ttyUSB*`) — expected, since WSL2 doesn't pass through USB by default. Flashing still needs to go through the Windows-side toolchain (COM5) unless `usbipd-win` is set up to bridge the board into WSL

### Decisions Made
- Kept both toolchains (Windows/winget and WSL/Linux) rather than trying to unify — they serve different purposes: WSL/background-job side for fast compile-checking without hardware, Windows/PowerShell side for actual flashing where the USB port is visible

### Blockers / Issues
- Local `main` checkout is stale (8 behind origin) and read-only from this job — needs a manual `git pull` from the user
- Still can't flash or capture serial output from this WSL session — no board attached; that requires either the existing Windows-side path or setting up `usbipd-win` USB passthrough (not done, not requested)

### Next Session
- User: `git pull` the local `main` checkout to pick up the wireless-variant work
- Everything from the 2026-07-12 (3) entry still applies: wire the RX module, capture real remote ON/OFF codes via `RF_Sniffer`, fill into `MagicBand_Wireless.ino` and `RF_TX_Test.ino`

-----

## 2026-07-12 (3) - RF Module Capture, Sniffer/TX Test Sketches

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Captured pinout/specs for the actual sourced 433MHz TX/RX modules (D-FLIFE kit, [Amazon B0BZRRBBNK](https://www.amazon.com/dp/B0BZRRBBNK)) from listing images + a detailed customer review — no formal datasheet exists for this board. New doc: `1. Project Management/RF Module Pinout - D-FLIFE 433MHz Kit.md`
- Key finding: this TX module is **3.3V-only** on its power pin, not 5V — folded into the wireless BOM (component table + power budget table) and into `MagicBand_Wireless.ino`'s pin-map header comment. TX now documented as sharing the MFRC522's 3.3V rail, not the 5V bus
- Wrote `RF_Sniffer/RF_Sniffer.ino` — recreates the bench-only RF capture tool referenced in Technical Reference (the original `Sniffer.ino` no longer exists in history, see prior session's Rev4 finding), using rc-switch's receive API. Prints code/bitlength/protocol/pulse-length over Serial for each remote button press
- Wrote `RF_TX_Test/RF_TX_Test.ino` — standalone TX bench tester; auto-sends a placeholder code every 3s (RCSwitch library example values, explicitly not a real outlet code) so a second board running the Sniffer can confirm the module is transmitting; also accepts `o`/`f` over Serial to send configurable ON/OFF codes once real ones are known
- All three sketches (`MagicBand_Wireless`, `RF_Sniffer`, `RF_TX_Test`) compile clean on both the Pro Mini and Nano FQBNs
- Flashed `RF_Sniffer.ino` to the spare Nano on COM5

### Decisions Made
- RF module data captured as its own PM doc rather than only inline in the BOM — it's reference material (pinout, review-sourced quirks) distinct from the BOM's role as a priced parts list
- Bench tools live in their own top-level sketch folders (`RF_Sniffer/`, `RF_TX_Test/`), matching the `MagicBand_Wireless/` convention rather than reviving the old `Revisions/` subfolder structure

### Blockers / Issues
- Can't verify RF_Sniffer's actual receive behavior from this session (same live-serial-capture limitation as the prior entry) — user needs to wire the RX module and watch Serial Monitor themselves
- RF_TX_Test's default codes are RCSwitch library example placeholders, not real — needs real captured codes before it's a meaningful test against the actual paired outlet

### Next Session
- User to wire RX module (long antenna, 5V, DATA→D2) and capture real remote ON/OFF codes via RF_Sniffer
- Once codes are captured: fill into `MagicBand_Wireless.ino` and `RF_TX_Test.ino`, flash TX test, confirm against the real outlet
- Everything else from the prior entry (order parts, bench-test power numbers) still applies

-----

## 2026-07-12 (2) - Wireless Variant: Firmware, Toolchain Setup

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Local project folder was empty at session start (no CLAUDE.md, PM files, or repo) — traced the repo to `git@github.com:zowen22/8.-Magic-Band.git`, set up SSH (generated ed25519 keypair, user added it to GitHub), and cloned onto `main`
- Installed `arduino-cli` (via winget), `arduino:avr` core, and `MFRC522`/`Adafruit NeoPixel`/`rc-switch` libraries
- Found a real doc/code mismatch: `4. Technical Reference.md` and the wireless BOM describe an RF-transmitter architecture (Rev4, pin 8, no mains relay) as current, but the surviving sketch (`MagicBand_v1.ino`) is relay-based — `Rev4.ino` was actually deleted in commit `b9a2e65` during a later cleanup, so its code no longer exists anywhere in history. User confirmed: build the wireless firmware on the RF-transmitter architecture (per docs), not the relay code that happens to still be in the repo
- Wrote `MagicBand_Wireless/MagicBand_Wireless.ino`: Pro Mini sleep/wake firmware per the BOM's "properly engineered" scenario — watchdog-driven `SLEEP_MODE_PWR_DOWN` waking ~1Hz, MFRC522 soft-powered up/down each poll, NeoPixel VCC gated through a MOSFET driver pin (fully off between animations, not just commanded black), RF transmit via rc-switch on card scan. DFPlayer kept optional behind `ENABLE_AUDIO`, consistent with BOM. RF codes are placeholders pending a real Sniffer capture
- Compiled clean for both target FQBNs: `arduino:avr:pro:cpu=16MHzatmega328` (real target) and `arduino:avr:nano:cpu=atmega328old` (only board physically on hand). 8838 bytes flash (28%), 444 bytes RAM (21%) on both
- Uploaded successfully to the spare Nano on COM5 (old-bootloader FQBN variant — avrdude synced and flashed cleanly, confirming the binary is valid and the toolchain/upload path works end-to-end)
- Could not get live serial output back through this session's Bash/PowerShell tooling — tested with both the real sketch (temporary heartbeat print, since reverted) and a trivial 2-line "ping" sketch; neither produced captured output via `arduino-cli monitor` in either shell. Successful avrdude sync/flash is still solid evidence the upload path works; this is a monitor/output-capture limitation in this environment, not a firmware issue

### Decisions Made
- Wireless firmware targets the RF-transmitter architecture, matching Technical Reference/BOM, not the relay code left in `MagicBand_v1.ino` — user's explicit call after I flagged the mismatch
- New sketch lives in its own folder (`MagicBand_Wireless/`), not overwriting `MagicBand_v1.ino` — wired and wireless builds stay side by side, matching the BOM doc's existing "alongside, not replacing" framing
- MFRC522 SS pin set to D4 in the new sketch (per Technical Reference's documented pin map) — note this doesn't match `MagicBand_v1.ino`'s actual SS=D10; that's a pre-existing mismatch in the wired build's own docs, out of scope tonight (flagged, not fixed)

### Blockers / Issues
- Both open BOM bench-test numbers (Lipo Rider Plus no-load quiescent current, Pro Mini active current) are still blocked — per WP2.3, the Pro Mini/Lipo Rider Plus/MOSFET driver/battery haven't been ordered yet, so there's no way to measure either figure this session regardless of tooling
- The spare Nano on COM5 was left flashed with `MagicBand_Wireless.ino` (compile/upload verification) rather than restored to its original wired-build firmware — user was asked, didn't respond before session wrap-up. Flagging for next session or whenever that board is needed for its original purpose
- Serial monitor output isn't capturable through this session's tools (see above) — if runtime behavior needs verifying without new hardware, that'll need the Arduino IDE Serial Monitor directly, not this CLI path

### Next Session
- Order Pro Mini, Lipo Rider Plus, MOSFET driver, battery (WP2.3, @user)
- Once parts arrive: bench-test both open power numbers, then flash `MagicBand_Wireless.ino` to the real Pro Mini and validate against the BOM's two runtime scenarios (~2-4 weeks engineered vs. ~18-20hrs unoptimized)
- Decide whether to restore the spare Nano's original firmware
- Run Sniffer against the paired outlet remote to fill in real `RF_CODE_ON`/`RF_CODE_OFF`/`RF_BITS`/`RF_PROTOCOL` values in `MagicBand_Wireless.ino` (currently placeholders)

-----

## 2026-07-12 - Wireless Variant: Power Architecture, Audit, Pricing

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Worked through PowerBoost 1000C stock-out: evaluated Seeed Lipo Rider Plus, SparkFun LiPo Charger/Booster, Adafruit PowerBoost 500C, DFRobot MP2636 — selected Lipo Rider Plus (USB-C, 2.4A headroom, in stock)
- Confirmed none of the recommended boost/charge boards have "smart power bank" auto-shutoff-at-low-load behavior (that's specific to phone-charging power bank ICs); confirmed over-discharge protection comes from the battery's own protection PCB, not the booster
- Corrected earlier "many days" runtime claim: continuous RFID scanning draws ~20hrs on a 2000mAh cell, not multi-day
- Modeled 1Hz duty-cycle polling: naive version (RFID gated, nothing else) barely helps, because Nano's onboard CH340 chip (5–20mA idle, doesn't sleep) and NeoPixels' off-state driver current (~0.6mA/pixel, confirmed via bench measurement + datasheet) dominate. Recommended swapping Nano → Pro Mini (drops CH340 floor) and MOSFET-gating NeoPixel (and previously DFPlayer) power
- Ran a mini audit on all consumption figures used this session, with sources and confidence levels; one unresolved gap — boost converter no-load quiescent current, needs bench test
- Priced the full BOM (parts + consumption per component)
- Dropped audio (DFPlayer + speaker + microSD) from the wireless variant's baseline BOM per user direction — kept optional in both the wired and wireless docs, consistent with existing Problem Statement scope, not fully removed
- Rewrote `BOM - Wireless Ornament Variant.md` to reflect all of the above: Pro Mini, Lipo Rider Plus, MOSFET gating, audio-as-optional, sourced power budget with two runtime scenarios, updated pricing
- Merged the branch to `main` per user request — working on `main` going forward, not the feature branch

### Decisions Made
- Lipo Rider Plus over PowerBoost 1000C (stock-driven, but also higher current headroom)
- Pro Mini over Nano for the wireless variant specifically — needed to hit meaningful battery runtime; wired-build Technical Reference still specifies Nano, unchanged
- Audio stays optional in both builds, not stripped from either — user's explicit call after I flagged the scope question
- Runtime estimates now split into "properly engineered" (~2-4 weeks) vs. "unoptimized fallback" (~18-20hrs) rather than a single number, since the gap is entirely firmware sleep/wake + MOSFET gating, not battery size

### Blockers / Issues
- Boost converter no-load quiescent current still unverified — could not extract a figure from TI's datasheet via search. Flagged in BOM doc; needs a bench measurement before the multi-week runtime estimate is trustworthy.
- Pro Mini active-current figure used (~15-20mA) is a general datasheet estimate, not independently re-verified this session

### Next Session
- Bench-test: Lipo Rider Plus no-load current, actual Pro Mini active current
- Write firmware: Pro Mini sleep/wake loop (~1Hz RFID poll) + MOSFET-gated NeoPixel power
- Check Lipo Rider Plus + battery physical dimensions against shell interior once WP2.2 progresses
- Order parts for prototype once above is validated

-----

## 2026-07-07 - Wireless Ornament Variant BOM

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Drafted `1. Project Management/BOM - Wireless Ornament Variant.md`: battery-powered variant of the ornament build, alongside (not replacing) the wired-build BOM in Technical Reference
- Core electronics (Nano, MFRC522, NeoPixel rings, RF transmitter, DFPlayer) unchanged from current architecture
- Added WP2.3 to Work Packages to track follow-up confirmation/validation

### Decisions Made
- Interpreted "wireless ornament" as untethering the ornament's power (removing barrel jack/wall adapter) — the RF link to the outlet is already wireless in the existing build. Flagged as an assumption pending user confirmation (AskUserQuestion tool failed mid-session due to a transient MCP disconnect, so proceeded with a documented default rather than blocking)
- Chose rechargeable LiPo + Adafruit PowerBoost 1000C (charger + boost in one board, USB-C) over a disposable AA/AAA pack or a separate TP4056+boost combo — fewer parts, no e-waste, single board handles charge/protection/boost
- Sized battery at 2000mAh as a starting assumption; flagged for revision once actual shell interior dimensions are known (Phase 2 enclosure work, WP2.2)

### Blockers / Issues
- `AskUserQuestion` tool call failed (stream closed / MCP disconnect) before user could weigh in on wireless scope, battery chemistry, and file placement — proceeded with defaults, clearly flagged in the new BOM doc and here

### Next Session
- User to confirm/correct the three flagged assumptions in the new BOM doc
- If battery chemistry changes to AA/AAA, rewrite BOM's power section accordingly
- Once shell interior dimensions are known, revisit battery capacity/form factor

-----

## 2026-06-12 - Framework: Audits + Branched Plans Support

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Added "Optional PM Artifacts" section to CLAUDE.md: Audits and Branched Plans conventions with lifecycle, naming, required fields, and rules
- Updated Session Start Routine (step 2) to check for open Audits/ and Plans/ sub-folders
- Added breadcrumb pattern: live "Open Audits / Open Plans" section in CLAUDE.md updated as artifacts open/close
- Validated in BetterGolfLeagueTracker project (first real use of Audits/ artifact type)

### Decisions Made
- Breadcrumb in CLAUDE.md (not in PM files) — ensures Claude loads it on every session without having to read all PM files first
- Branched Plans archived (not deleted) when resolved so decision history is preserved

### Blockers / Issues
- None

### Next Session
- N/A — framework update is complete; future sessions use this in real projects

-----

## 2026-06-16 - Project Kickoff, Code Review + Architecture

*Status: `COMPLETED`*
*Type: `Manual`*

### What Was Done
- Cloned 8.-Magic-Band repo, copied all Arduino sketches from OneDrive
- Populated all 5 PM files from conversation context
- Full code audit of Rev2/FINAL — identified 8 bugs (see Technical Reference)
- Wrote Rev3: clean rewrite fixing all bugs, audio re-integrated, pin conflict resolved
- Wrote Sniffer.ino: one-time RF code capture tool
- Wrote Rev4: production sketch replacing relay with 433MHz RF transmitter
- Finalized architecture: Arduino Nano, barrel jack power, RF outlet, no floor box, no mains wiring

### Decisions Made
- 433MHz RF outlet replaces relay + floor box — no mains wiring on product side
- Arduino Nano over Uno — smaller, same pins, fits ornament
- 5.5mm/2.1mm panel-mount barrel jack on ornament shell
- DFPlayer kept, moved to pins 2/3 — pin conflict fully resolved
- Pixel counts as `NUMPIXELS_OUTER` / `NUMPIXELS_INNER` constants — confirm on hardware

### Blockers / Issues
- Hardware in transit — can't test until it arrives
- Pixel counts unconfirmed

### Next Session
- Run Sniffer, capture RF codes from outlet remote
- Fill RF_CODE_ON / RF_CODE_OFF into Rev4
- Confirm pixel counts, update constants
- Test Rev4 end-to-end

-----
