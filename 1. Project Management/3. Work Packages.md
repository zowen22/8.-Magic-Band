# Work Packages

*Progress: [46 / 61 tasks complete]*

-----

## Phase 1: Code Cleanup *(In Progress)*

### WP1.1 - Code Review & Bug Inventory
- [x] @claude - Audit Rev2/FINAL for bugs and code quality issues (done: 2026-06-16)

### WP1.2 - Firmware Rewrite (Rev3)
- [x] @claude - Replace verbose pixel loops with for-loops and `fill()`
- [x] @claude - Fix relay toggle duplication — single `toggleRelay()` helper
- [x] @claude - Replace counter parity with `TreeOn` boolean
- [x] @claude - Fix `rainbowstill()` uninitialized variable `j`
- [x] @claude - Fix dead code in `Wheel()` (rewritten with clean ColorHSV)
- [x] @claude - Pixel counts as `NUMPIXELS_OUTER`/`NUMPIXELS_INNER` defines — easy to update
- [ ] @claude - Add UID whitelist (optional `#define UID_FILTER` — future)

### WP1.3 - Audio (DFPlayer)
- [x] @claude - Move RFID SS to pin 4, DFPlayer to pins 2 & 3 — all conflicts resolved
- [x] @claude - Re-integrate DFPlayer in Rev3 with graceful fallback if not detected

-----

## Phase 2: Hardware Improvements *(Future)*

### WP2.1 - Physical
- [ ] @user - Document actual pixel counts and ring layout (confirm NUMPIXELS1/2 match hardware)
- [ ] @user - Confirm relay type and 120V wiring safety (normally-open vs normally-closed)

### WP2.2 - Enclosure
- [ ] @user - Design or source ornament enclosure with room for Arduino + RFID + pixel rings
- [ ] @user - Design or source floor power box enclosure for relay

### WP2.3 - Wireless Ornament Variant *(PAUSED 2026-08-04 -- LiPo shipping/Etsy listing risk, see Decisions Log. Not abandoned, may revisit.)*
- [x] @claude - Draft BOM for battery-powered ornament variant (done: 2026-07-07)
- [x] @claude - Confirm wireless-scope (battery power) and switch to LiPo + charge/boost board (done: 2026-07-12)
- [x] @claude - Audit consumption figures, price full BOM, drop audio from baseline (done: 2026-07-12)
- [ ] @user - Bench-test Lipo Rider Plus no-load quiescent current before trusting multi-week runtime estimate (blocked: part not yet ordered)
- [ ] @user - Check Lipo Rider Plus + LiPo cell dimensions against shell interior
- [x] @claude - Write firmware: Pro Mini sleep/wake loop (~1Hz RFID poll) + MOSFET-gated NeoPixel power (done: 2026-07-12, `MagicBand_Wireless/MagicBand_Wireless.ino` — compiles clean for Pro Mini, uploaded successfully to a spare Nano to confirm bootloader/flash path; full runtime behavior unverified, no Pro Mini on hand yet)
- [ ] @user - Order Pro Mini, Lipo Rider Plus, MOSFET driver, battery for prototype
- [ ] @user - Bench-test actual Pro Mini active-current draw once ordered (blocked: part not yet ordered)
- [x] @claude - Capture D-FLIFE 433MHz TX/RX module pinout, fold 3.3V TX finding into BOM (done: 2026-07-12)
- [x] @claude - Write `RF_Sniffer.ino` + `RF_TX_Test.ino` bench tools, flash Sniffer to spare Nano (done: 2026-07-12)
- [x] @user - Capture real remote ON/OFF codes via RF_Sniffer (done: 2026-08-03 — top-row outlet, ON `0x885A8F00` / OFF `0x845A8F00`, 32-bit, protocol 2)
- [x] @claude - Fill captured codes into `MagicBand_Wireless.ino` + `RF_TX_Test.ino`, verify compiles clean (done: 2026-08-03)
- [ ] @user - Flash `RF_TX_Test.ino`, confirm it fires the real outlet ON/OFF before wiring TX into the ornament build

-----

## Phase 3: Custom PCB -- Battery Variant *(PAUSED 2026-08-04, see Decisions Log)*

Schematics/research preserved as reference -- RFID/RF/NeoPixel wiring and connector-fit findings carry forward to whatever board follows. PCB layout work was not resumed here.

### WP3.1 - PCB Design Onboarding
- [x] @claude - Decide tool (KiCad) and approach (carrier/breakout board, not fully custom/integrated) (done: 2026-08-04)
- [x] @claude - Deep-dive research: KiCad workflow, footprints, JLCPCB DFM/assembly, RF antenna keepout on a carrier board (done: 2026-08-04, see `PCB Design Plan.md`)
- [x] @claude - Deep-dive research: fully-custom (bare ATmega328P) feasibility, footprints, MFRC522/RF-TX IC-level feasibility (done: 2026-08-04, see `PCB Design Plan.md`)
- [x] @claude - Build v1 carrier-board schematic in KiCad, ERC-clean (done: 2026-08-04, `PCB/MagicBand_Carrier/`)
- [x] @claude - Build v2 fully-custom (bare ATmega328P) schematic in KiCad, ERC-clean, netlist-verified against design intent (done: 2026-08-04, `PCB/MagicBand_FullyCustom/`)
- [x] @user - Set up sym-lib-table/fp-lib-table for the PCB project (done: 2026-08-05, one-time GUI first-launch wizard, accepted bundled local libraries)
- [x] @claude - PCB layout placement: 40x40mm target MET, all 20 components placed zero-overlap, front/back + antenna keepout constraints respected (done: 2026-08-04, see PCB Design Plan.md 1.7)
- [x] @claude - **Schematic/PCB change**: removed J7 (was inner-ring NeoPixel connector) entirely -- user is sourcing a single LED strip for the outer position only, no second ring (decided 2026-08-06, done same day). Renamed J6's schematic value from `NeoPixel_Outer_16px` to `NeoPixel_LED_Strip` (count-agnostic, since actual pixel count is TBD pending the strip order). Done via direct S-expression edit (KiCad was closed by the time this happened, no live-file conflict) rather than the GUI, verified rather than assumed clean: ERC 0 errors (129 warnings, down from 209, all pre-existing off-grid cosmetic), DRC 0 unconnected pads / 0 footprint errors (188 violations, down from 202), also removed 2 dangling PCB track stubs that had routed to J7's old position with nothing on the other end. Confirmed side benefit: `npth_inside_courtyard` dropped from 3 to 2 -- the MH3/J7 courtyard conflict is gone, only MH3/J6 and the separate pre-existing MH1/J8 one remain. Schematic/PCB/ERC/DRC reports/SVG+PDF exports all regenerated and consistent
- [x] @claude - **Placement finalized ahead of routing**: fixed the MH3/J6 courtyard conflict (moved MH3 from (37,20) to (37,27), the one error-level placement issue) and 2 of 3 leftover silkscreen label overlaps (R1, C4) from the 2026-08-05 reference-designator un-hide pass (done: 2026-08-07). DRC: 188 -> 176 violations. `npth_inside_courtyard` down to 1 (only the separate pre-existing MH1/J8 flag remains). U2's label still overlaps J2's silkscreen -- tried 4 offsets, this row (C1-U2-C2, J2 above, Q1 below) has no headless-computable clean spot, needs a GUI nudge
- [x] @user - **Placement/pre-route cleanup, GUI session** (done: 2026-08-09): repositioned J8, fixed U2/J2 silkscreen overlap, deleted J3's antenna keepout zone (was blocking FreeRouting from placing vias/tracks near it -- will keep other copper off the antenna's near-field manually instead of via a formal keepout), ripped up all leftover tracks/vias from the earlier unsaved routing attempt, refilled copper zones. DRC: 176 -> 4 violations (remaining: silk-clipped-at-edge on MH1's label, 3 pre-existing "footprint not in library" path warnings, all cosmetic/non-blocking) + 42 unconnected pads (expected -- board has no routing yet, ready for a fresh FreeRouting pass)
- [x] @user - **PCB layout routing, persisted this time** (done: 2026-08-09): DSN export (GUI) -> FreeRouting run headlessly (jar + JDK 25 JRE, `-de/-do/-mp 50`, see `4. Technical Reference.md`) -- 42 unrouted nets -> **0**, converged in 4 passes in ~2.5s (previous 2026-08-05/06 attempt stalled at 2 unrouted nets at J3, before its antenna keepout was removed). Specctra session re-imported and saved, zones refilled. DRC: 8 -> 52 (transient, stale zone fill mid-import) -> 8 after refill. 4 of those 8 are pre-existing cosmetic (MH1 silk-edge clip, 3 footprint-library path warnings); 4 are real and left open below
- [x] @user - Set J2 pad 6, J9 pad 6, U1 pad 22 [GND] to solid zone connection via pad Properties (done: 2026-08-09, GUI). DRC: 8 -> 6. **J2 pad 6 fully resolved.** J9 pad 6's thermal flag cleared but it's still unconnected -- not a broken track, user's working theory (plausible, unconfirmed) is it's bonded to an isolated pocket of GND pour rather than the main network. U1 pad 22 unchanged -- solid setting likely didn't save for this pad, needs re-check
- [x] @claude - J9 footprint reassigned to a shrouded/keyed IDC-6 box header in the schematic (done: 2026-08-10) -- for repeatable ISP programming via a single keyed ribbon cable instead of 6 loose jumper wires each time. ERC still clean (0 errors, 129 pre-existing warnings)
- [x] @user - **J9 PCB placement + re-route, done** (2026-08-10): swapped the PCB footprint via Change Footprint(s) (`Connector_IDC:IDC-Header_2x03_P2.54mm_Vertical`), moved to a spot with room for the shroud, cleared all tracks/vias, re-exported DSN, re-ran FreeRouting (42 -> 0 unrouted, ~2.3s), refilled zones. **Note: "Update PCB from Schematic" (F8) is broken for this board** -- PCB footprints are missing the `path` linkage back to schematic symbol UUIDs (likely from earlier direct S-expression edits bypassing the normal netlist-import flow), so F8 can't match existing footprints and duplicates every part instead. Don't use F8 on this board; use Change Footprint(s) directly on the PCB side for any future footprint swaps
- [x] @user - **U1 pad 22 and J9 pad 6 resolved** (2026-08-10): a later placement move caused FreeRouting to stall at 6 unrouted nets; user manually rerouted the affected connections using interactive routing with mid-trace layer swap (**X** to route, **V** to drop a via and hop layers), re-exported DSN, confirmed via FreeRouting (0 unrouted to add), re-imported. DRC: 6 -> 5 violations, 3 -> 1 unconnected. Both pads gone from the report
- [x] @user - **U1 pad 8 resolved -- all 3 GND issues now fixed** (2026-08-11): pour was split into two disconnected islands by a pinched-clearance gap between them (pad 8 only touched the smaller, isolated one). Fixed with a short manual GND track bridging pad 8 directly to the adjacent valid pour. DRC: 0 unconnected pads. A residual `starved_thermal`/isolated-island *flag* remains on this pad (the pour's own fill geometry still shows two separate islands, since the track didn't widen the actual copper corridor) -- cosmetic only, not a functional defect, since the net is electrically complete via the track. Fixing that too (for a fully clean DRC) is optional, not required
- [x] @user - Confirm ornament shell interior dimensions (done: 2026-08-11) -- shell fits up to a ~45mm square; staying closer to 40x40mm by choice, not a hard constraint
- [x] @claude - **Adversarial PDR #2, fresh independent agent** (done: 2026-08-11): re-verified ERC/DRC independently rather than trusting prior claims. Found 1 blocking issue and 4 should-fix items -- see `5. Session Log.md` for full findings. Blocking: RF antenna keepout (deleted 2026-08-09 to unblock FreeRouting near J3) was never actually being honored -- GND pour/vias sit 0.0-1.4mm from J3's courtyard vs. the 3-5mm spec, ~8-10x tighter than required. Should-fix: U1 placement didn't match the documented front/back constraint (design plan's own self-report was wrong -- never actually verified before); orientation-label silkscreen text was under the min-height rule; mounting holes had thin edge margin; U1's decoupling caps sit inside the RF general-clearance guidance
- [x] @user - Fixed 2 of 4 should-fix items (2026-08-11/12): bumped label text to 1mm height (stroke width left at 0.1mm, not bumped to the recommended 0.15mm -- worth revisiting); widened mounting-hole edge margin from ~1.4mm to ~2.4mm (re-verified sufficient)
- [x] @user - **Decision: U1 does not need to be at the board's front** -- re-centered U1 for maximum edge clearance in both directions instead of forcing it toward J2/the front per the original (never-actually-verified) design-plan constraint. Explicit call: no strong reason found for the original constraint, so it's dropped in favor of clearance
- [ ] @user - **Decision: RF antenna proximity issue explicitly deferred, not fixed** -- noted and accepted as a known risk (possible reduced/dead 433MHz range) rather than reworked now. Revisit if real-world range problems actually show up during testing. If revisited: needs either a real keepout zone restored (with reroute) or the GND pour/vias/decoupling caps near J3 manually pulled back 3-5mm
- [x] @user - Silkscreen stroke width on "Right"/"Top" labels fixed to 0.15mm, matching the board's standard (confirmed by PDR #3 -- was stale as still-open on this list)
- [x] @user - **U1 pad 8 and pad 22 re-fixed at the new position** (2026-08-12): same manual-bridge-track technique as before. DRC: 5 -> 3 violations, 1 -> 0 unconnected. Only the 3 pre-existing footprint-library warnings remain (MH1-3, cosmetic, not fixable from the GUI). This time no residual starved-thermal flag either -- fully clean on the electrical/connectivity front. The silk_overlap between the "Right" label and U1's outline (introduced by the recenter) is also gone. **Board is DRC-clean except for the 3 unfixable cosmetic library-path warnings**
- [x] @claude - **Adversarial PDR #3, fresh independent agent** (done: 2026-08-12): re-verified ERC/DRC and every fix from PDR #2 -- all held. Flagged J2 as apparently centered rather than at the front, based on a Y-axis front/back assumption that turned out to be wrong (see decision below) -- false positive, not a real issue. Real findings: Y1 (crystal) only ~0.70mm from the right edge (DRC doesn't check courtyard-to-edge); RF antenna clearance confirmed unchanged since 2026-08-11; a dangling `D6` ERC label surviving the 2026-08-06 J7 removal; a duplicate GND track at U1 pad 8; a misapplied zone-connect override on U1's unused pad 2 (not pad 22 as intended)
- [x] @user - **Decision: front/back is the board's X-axis, not Y** -- corrected via the board's own "Front"/"Right" silkscreen labels. Closes out the J2-placement finding as a false positive (J2 was at the front all along); documented in `PCB Design Plan.md` §1.4 so future reviews don't repeat the same axis mixup
- [x] @user - **Y1 relocated for edge clearance** (2026-08-12), moved off the ~0.70mm-margin spot PDR #3 flagged. Cleared/manually cleaned up duplicate tracks (more than just the one PDR #3 found -- used Route -> Cleanup Tracks and Vias) and finished routing by hand, no FreeRouting import used. DRC: 0 unconnected, 4 violations (3 pre-existing cosmetic + 1 residual cosmetic starved-thermal flag on U1 pad 8)
- [ ] @user - Clear the dangling `D6` global label in the schematic (leftover from the 2026-08-06 J7 removal, low-severity ERC warning, pin genuinely unused)
- [ ] @user - Clear the misapplied zone-connect "solid" override on U1 pad 2 (unused pin, harmless but incorrect -- was meant for pad 22 back on 2026-08-09)

-----

## Phase 4: Custom PCB -- Barrel-Jack/Wall-Powered Variant *(In Progress)*

### WP4.1 - Scope
- [x] @claude - Re-derive schematic from the paused v2 design, dropping battery-specific complexity (MOSFET NeoPixel gating removed; sleep/wake loop was firmware-only, not schematic, still to be addressed in `.ino`) (done: 2026-08-04, `PCB/MagicBand_BarrelJack/`, ERC-clean)
- [x] @claude - Add barrel jack power input, 5.5mm/2.1mm center-positive (done: 2026-08-04 -- caught and reverted a real short from a KiCad symbol pin-geometry mismatch before committing, see PCB Design Plan.md)
- [x] @claude - Write `MagicBand_BarrelJack.ino`: new sketch derived from `MagicBand_Wireless.ino`, sleep/wake watchdog loop and MOSFET gating calls removed, compiles clean (done: 2026-08-04, PDR-driven -- the leftover ~1s poll delay was flagged as a real UX regression, not just stale code)
- [x] @claude - PDR (adversarial review by a fresh agent): found and fixed 1 blocking issue (no reverse-polarity/overvoltage protection on barrel jack input -- added P-channel MOSFET ideal-diode circuit, Q1/R2) plus 5 should-fix items (orphaned D7 label, layout-constraint task linkage, MFRC522/ISP shared-SPI-bus gotcha documented, MCP1700-keep decision made explicit, doc typo fixed) (done: 2026-08-04, see PCB Design Plan.md)
- [ ] @claude - PCB layout for the barrel-jack board -- MUST follow the "Physical Layout Constraint" section in `PCB Design Plan.md` (RFID/MCU stack at ornament front, barrel jack wiring at rear)
- [ ] @user - Confirm ornament shell interior dimensions to set board outline target (still open, ties to WP2.2)

