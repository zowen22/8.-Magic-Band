# Work Packages

*Progress: [1 / 10 tasks complete]*

-----

## Phase 1: Code Cleanup *(In Progress)*

### WP1.1 - Code Review & Bug Inventory
- [x] @claude - Audit Rev2/FINAL for bugs and code quality issues (done: 2026-06-16)

### WP1.2 - Firmware Rewrite (Rev3)
- [ ] @claude - Replace verbose pixel loops with for-loops and `fill()`
- [ ] @claude - Fix relay toggle duplication — single `toggleRelay()` helper
- [ ] @claude - Replace counter parity with `TreeOn` boolean
- [ ] @claude - Fix `rainbowstill()` uninitialized variable `j`
- [ ] @claude - Fix dead code in `Wheel()` (unreachable `pixelsIn` block after return)
- [ ] @claude - Correct pixel count mismatches (NUMPIXELS1=16, NUMPIXELS2=11; code addresses up to index 23)
- [ ] @claude - Add UID whitelist (optional, off by default — can enable via `#define`)

### WP1.3 - Audio (DFPlayer) — Deferred
- [ ] @user - Decide: move RFID SS to a different pin (e.g. pin 4) to free pin 10 for DFPlayer RX
- [ ] @claude - Re-integrate DFPlayer once pin conflict resolved

-----

## Phase 2: Hardware Improvements *(Future)*

### WP2.1 - Physical
- [ ] @user - Document actual pixel counts and ring layout (confirm NUMPIXELS1/2 match hardware)
- [ ] @user - Confirm relay type and 120V wiring safety (normally-open vs normally-closed)

### WP2.2 - Enclosure
- [ ] @user - Design or source ornament enclosure with room for Arduino + RFID + pixel rings
- [ ] @user - Design or source floor power box enclosure for relay
